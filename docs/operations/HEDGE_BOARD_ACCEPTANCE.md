# 对冲基金看板验收手册

> 适用基线：Platform 0.9.1 候选版本  
> 任务：Issue #134  
> 分支：`feature/issue-134-platform-0-9-1-unified-delivery`  
> 状态：自动化验收、交易时段与非交易时段真实Provider样本已完成；实际环境状态展示、真实链接和人工视觉验收仍需形成证据。

本地完整执行步骤、独立worktree、安全边界、日志目录和验收报告模板见：

```text
docs/operations/PLATFORM_0_9_1_LOCAL_ACCEPTANCE_HANDOFF.md
```

## 1. 验收边界

本手册覆盖：

- A股市场表现、市场广度、市场明细；
- 申万一级、申万二级与个股成交额阈值统计；
- 短线情绪；
- 账号级自选股；
- 一键个股数据；
- 宏观事件概率；
- 外部免费研究数据源的状态、降级和最后有效数据。

本手册不覆盖：

- 真实下单、订单、成交、持仓和会计账本；
- Execution Runtime；
- 将投研行情认定为交易执行权威行情；
- 对第三方免费数据源的永久可用性承诺。

## 2. 自动化验收

### 2.1 后端与结构检查

```powershell
cd platform-backend
python -m ruff check app tests scripts
python -m pyright
python -m pytest

cd ../admin-risk
npx pnpm@9.15.9 test:hedge-board-layout
npx pnpm@9.15.9 type:check
npx pnpm@9.15.9 build
```

### 2.2 对冲基金看板浏览器E2E

```powershell
cd admin-risk
$env:E2E_CEO_USERNAME="e2e_ceo"
$env:E2E_CEO_PASSWORD="使用本地临时强密码"
npx pnpm@9.15.9 test:e2e:hedge-board
```

浏览器E2E使用真实登录、真实账号自选股API和真实数据库持久化；仅对不稳定的外部研究数据接口使用确定性夹具，不以夹具结果冒充真实数据源验收。

浏览器E2E还会在以下视口生成分区截图并检查页面级横向溢出：

- 1440 × 900；
- 1024 × 768；
- 768 × 1024；
- 390 × 844。

每个视口分别生成“大盘表现”“申万板块”“自选股”三张截图。截图位于`test-results/hedge-board`工作流产物中。测试会先关闭窄屏导航遮罩，并等待瞬时错误提示消失，避免将过渡态误当作最终页面。该步骤属于自动化响应式预检，不能替代所有者对信息密度、可读性和业务体验的人工确认。

## 3. 真实研究数据探测

### 3.1 运行中的Platform API

先启动Platform Backend，再执行：

```powershell
cd platform-backend
python scripts/validate_research_sources.py `
  --base-url http://127.0.0.1:8000 `
  --stock-code 600519 `
  --threshold-yuan 10000000000 `
  --output ../artifacts/hedge-board-live-source-check.json
```

启用生产API Key时使用：

```powershell
python scripts/validate_research_sources.py --api-key "<临时注入，不写入仓库>"
```

使用浏览器会话时，可传入临时会话Token：

```powershell
python scripts/validate_research_sources.py --session-token "<临时会话Token>"
```

该脚本只通过Platform API检查：

- API契约；
- `source`、`sourceTimestamp`、`fetchedAt`；
- `ready / partial / stale / no_data / error`；
- 模块和行数；
- 申万未匹配证券数量。

脚本不能替代人工判断价格和市场口径是否正确。

### 3.2 免费Provider独立探测

```powershell
cd platform-backend
python scripts/smoke_research_providers.py --timeout 60
```

该探针记录上海市场时间、交易状态、每项Provider结果、响应耗时和贵州茅台腾讯/东方财富双源报价。外部失败为非阻塞证据，但必须准确标记为`partial`，不得描述为全部健康。

Provider独立探针证明上游在指定时点的返回情况，不证明运行中的Platform页面已正确展示`stale`、上游时间、平台抓取时间或Last Known Good；这些仍需通过实际运行环境检查。

## 4. 已记录的真实Provider样本

### 4.1 交易时段样本

- 时间：`2026-07-30 14:47:41 +08:00`；
- 市场状态：`trading`；
- 工作流：Research Provider Smoke `30520684588`；
- 结果：8项检查中5项通过、3项失败，总体`partial`；
- 通过：市场广度、短线情绪、腾讯600519报价、东方财富600519报价、双源一致性；
- 双源价格：腾讯`1358.23`、东方财富`1358.20`，绝对差`0.03`；
- 失败：全市场现货连接被远端中断、指数历史不可用、申万官方批量文件TLS证书链不可验证；
- 安全处理：未关闭TLS校验，失败由模块级状态和Last Known Good承接。

### 4.2 非交易时段样本

- 时间：`2026-07-30 15:42:05 +08:00`；
- 市场状态：`non_trading`；
- 工作流：Research Provider Smoke `30523771312`；
- 结果：8项检查中5项通过、3项失败，总体`partial`；
- 通过：市场广度、短线情绪、腾讯600519报价、东方财富600519报价、双源一致性；
- 双源价格：腾讯`1361.76`、东方财富`1361.76`，绝对差`0.00`；
- 行情日期：市场广度与短线情绪均返回`2026-07-30`；
- 失败与交易时段一致：全市场现货连接被远端中断、指数历史不可用、申万官方批量文件TLS证书链不可验证；
- 结论：收盘后两个独立报价源一致，但免费上游仍处于部分可用状态；本样本不替代运行中Platform的`stale`和Last Known Good展示验收。

## 5. 人工业务验收

### 5.1 交易时段

- [ ] 大盘表现至少返回上证指数，并保留中证2000。
- [ ] 收盘价、涨跌幅和成交额与一个可信行情终端抽样核对。
- [ ] 20日波动率抽样取一项，用21个有效收盘价复算。
- [ ] 上涨、下跌、平盘家数之和与市场覆盖范围相符。
- [ ] 涨停、真实涨停、炸板、跌停和连板梯队与当日市场抽样核对。
- [ ] 申万二级默认按成交额Top 10展示。
- [ ] 全部申万二级支持一级筛选、名称/代码搜索和升降序。
- [ ] 50亿元、100亿元、200亿元和自定义阈值均严格使用`>`。
- [ ] 点击行业数量能展开具体股票。
- [ ] 复制统计与CSV导出的数量、行业和口径一致。
- [ ] 未匹配申万分类的证券有明确记录，非零结果逐项核查。

### 5.2 非交易时段

- [ ] 数据时间明确显示上游时间和平台抓取时间。
- [ ] 不将上一交易日数据伪装为实时数据。
- [ ] `stale`状态清晰可辨。
- [ ] 暂无数据与接口错误的视觉状态不同。
- [ ] 空拉取不覆盖最后一份有效数据。

### 5.3 一键个股数据

至少抽样：沪市主板、深市主板、创业板各一只。

- [ ] 支持`600519`、`SH600519`、`600519.SH`等输入并归一化。
- [ ] 新查询后所有数据模块默认折叠。
- [ ] 某一数据源失败不导致整页失败。
- [ ] 行情、财务、估值、公告、研报等模块显示真实来源。
- [ ] 研报、公告和新闻原文链接能够打开。
- [ ] 连续快速切换股票时，旧请求不覆盖最后一次查询。

### 5.4 账号级自选股

- [ ] 添加、删除、分组和组内排序后显示“账号已同步”。
- [ ] 明确保留空自选股列表。
- [ ] 关闭当前浏览器会话，在新浏览器会话登录同一账号后恢复数据。
- [ ] 不同账号的自选股相互隔离。
- [ ] 后端临时不可用时，本地缓存仍可使用并显示“本地缓存·待同步”。
- [ ] 后端恢复后，本地变更可重新同步。

### 5.5 视觉与响应式

分别检查1440px、1024px、768px和390px宽度：

- [ ] 查看自动化生成的“大盘表现”“申万板块”“自选股”分区截图。
- [ ] 表格横向滚动不破坏页面布局。
- [ ] 数值右对齐，名称左对齐，空值统一为“—”。
- [ ] 红涨绿跌符合平台约定。
- [ ] 筛选、应用、复制、导出和展开操作易于识别。
- [ ] 页面没有重叠、截断、白屏或明显布局跳动。

## 6. 验收记录

| 环境 | 日期 | 执行人 | 交易状态 | 自动化结果 | 真实数据结果 | 视觉结果 | 结论 |
|---|---|---|---|---|---|---|---|
| GitHub Actions免费Provider探针 | 2026-07-30 | 自动化 | 交易时段 | 工作流通过 | `partial`，5/8通过 | 不适用 | 外部依赖风险已记录 |
| GitHub Actions免费Provider探针 | 2026-07-30 | 自动化 | 非交易时段 | 工作流通过 | `partial`，5/8通过 | 不适用 | 双源收盘价一致，外部风险持续 |
| 实际运行环境状态检查 | 待填写 | 待填写 | 非交易时段 | 待填写 | 待填写 | 待填写 | 待填写 |
| 响应式截图人工复核 | 待填写 | 所有者 | 不适用 | 自动化分区截图待复核 | 不适用 | 待填写 | 待填写 |

## 7. 发布判定

只有同时满足以下条件，才可将0.9.1候选版本标记为可发布：

1. Platform CI、User System Browser E2E、Hedge Board Browser E2E、Secret Scan和Version Consistency全部通过；
2. 交易时段和非交易时段的真实Provider验收各完成一次；
3. 实际运行环境中的来源时间、`stale`和Last Known Good展示完成检查；
4. 账号级自选股跨会话恢复通过；
5. 无高优先级视觉或业务缺陷；
6. 所有者明确批准合并。

在此之前，相关PR保持Draft并且不得合并到`main`。