# Task: External Read-Only Connection Verification

Task ID: `VG-0111-EC-01`
Issue: `#none`
Status: `in_progress`
Last transition at: `2026-08-18 21:00 Asia/Shanghai`
Owner notice: `sent`
Risk level: `critical`
Role: `implementation`
Context Pack: `live-readiness`

## Objective

Verify real external read-only connections to the Bybit perpetual venue (`XAUTUSDT`, linear/USDT) and the Bybit-provided MT5 gold venue (`XAUUSD+`), and shadow-reconcile the returned venue evidence against Platform facts. This is the first step of the `LIVE_ACCEPTANCE_RUNBOOK.md` release sequence (只读实盘) and a natural prerequisite for the separately authorized controlled-live acceptance (business plan items 9/10).

## Protected semantics

- **Read-only only**: no order submission, no cancel, no Live Write, no state change on any venue.
- `live_write_enabled` stays `false`; `bybit_demo_mode` stays `false` (real account).
- Credentials are never copied into source control, logs or documentation; they resolve from `secret://environment/bybit-live-001` and `secret://environment/mt5-live-001`.
- Any failed, unavailable or contradictory step returns the system to read-only and requires an accountable operator decision.

## Scope

- In scope: start the execution-runtime in `bybit_mt5` + `live` read-only mode; pass `scripts/live-readonly-preflight.ps1` (health → status → capabilities → credentials → venue readiness); query venue balances/positions/orders/fills/economic events; shadow-reconcile against Platform facts.
- Non-goals: Live Write, order submission, controlled-live execution, frontend changes, production deployment.

## Implementation fields

- Implementation owner: `techlead-ec01`
- Runtime gateway: `bybit_mt5`, environment `live`, `live_write_enabled=false`
- MT5 terminal path: `C:\Program Files\Bybit MT5 Terminal\terminal64.exe`
- Runtime port (verification instance): `8101` (the `fake`/development instance on `8100` is untouched)
- Bybit symbol: `XAUTUSDT`; MT5 symbol: `XAUUSD+`

## Verification

- `scripts/live-readonly-preflight.ps1 -RuntimeBaseUrl http://127.0.0.1:8101`
- Runtime `/status` = `environment=live`, `gateway=bybit_mt5`
- `/gateway/connectivity` = `configuredCredentialCount == credentialCount`
- `/gateway/venue-readiness` = both venues `ready`
- Balances/positions/orders/fills queried and shadow-reconciled against Platform facts

## Progress

- Done: MetaTrader5 5.0.6090 installed into the execution-runtime venv; Bybit API key (real account) and MT5 credentials verified present in the environment.
- Current: starting the live read-only runtime and running the preflight.
- Next: venue evidence query + shadow reconciliation.
- Blocked by: none.
