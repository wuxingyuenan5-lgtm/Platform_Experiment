# Task: Funding-Carry (Perpetual-Short + Spot-Long) Complete Local Closed Loop
Task ID: `VG-0111-FC-01`
Issue: `#none`
Status: `attention`
Last transition at: `2026-08-18 13:40 Asia/Shanghai`
Owner notice: `sent`
Business status summary: `Needs: owner reviews the funding-carry local closed-loop receipt. Capability: 0.11.1 资金费策略完整本地闭环 closed — CEO-specified perpetual-short plus spot-long pair on one Bybit account through the real Platform API -> Execution Runtime -> deterministic FakeGateway path: perpetual leg first, spot hedge released only after the confirmed perpetual fill, hedged open, close, positions returning to zero, exact platform<->venue reconciliation, and fail-closed behavior when the runtime is unreachable. Evidence: funding adapter, batch hedge hook, E2E tests (2 passed). Next gate: owner authorization for external read-only connection verification (待授权) or the final independent acceptance.`
Current leaf task/agent ID: `none`
Risk level: `critical`
Role: `implementation`
Agent ID: `techlead-fc01`
Context Pack: `funding-data`
Token baseline: `unavailable`
Token current: `unavailable`
Token delta: `unavailable`
Control-plane token delta: `unavailable`
Token budget: `2000000`
Token status: `unavailable`

## Objective
Close the funding-carry local closed loop: implement and locally verify the CEO-specified perpetual-short plus spot-long pair on one Bybit account with perpetual-leg-first execution, spot hedge released only after the authoritative perpetual fill (proportional to the confirmed fill), close, exact platform<->venue reconciliation, and fail-closed failure handling, reusing the accepted chase/incremental-hedging, serialization, notional-alignment and unified-ledger foundations.

## Implementation fields
- Implementation owner: `techlead-fc01`
- Branch: `codex/vg-0111-fc-01`
- Worktree: `.wt-test`
- Base commit: `c984cb8946294b7334756fdd42ea1dbb17476f16`

## Protected semantics
- Instruction-bounded quantity, one-batch identity, cumulative-fill ceilings, idempotency, Kill Switch, fail-closed unknown handling, reconciliation and read-only reset remain mandatory.
- Live Write stays disabled; the runtime runs with the FakeGateway and live_write_enabled=False.
- No external venue, credentials, real funding settlement or real-order operations.

## Scope
- In scope: `platform-api/app/funding.py` (funding market command adapter), `FundingMarketCommandRequest` schema, `execution_batches` funding hedge hook (`resize_funding_spot_hedge`), trading route, and the E2E closed-loop test `platform-api/tests/test_funding_local_closed_loop.py`.
- Non-goals: automatic symbol selection or sizing, reverse carry, cross-venue funding carry, real funding settlement (deferred to the separately authorized live acceptance gate), frontend.

## Dispatch concurrency decision
- Write set: `platform-api/app/{funding,execution_batches,execution_schemas,trading_routes}.py; platform-api/tests/test_funding_local_closed_loop.py; docs/codex/0.11.1-program.md; docs/codex/tasks/VG-0111-FC-01.md`
- Shared workflow, public contract, migration chain or file set: `0.11.1 funding-carry execution`
- Dependencies: `VG-0111-SF-01`, `VG-0111-SF-02`, `VG-0111-LR-01`, `VG-0111-FN-01`, `VG-0111-LED-01`, `VG-0111-CS-02`
- Independent test: `python -m pytest platform-api/tests/test_funding_local_closed_loop.py` (2 passed) against a real runtime subprocess with FakeGateway
- Rollback boundary: `reset to c984cb8`
- Parallel decision: `serial`
- Parallel with: `none`
- Parallel peer write set: `none`
- Independence evidence: `single implementation owner; no unfinished dependency`
- Acceptance task: `none (owner reviews receipt)`
- Active-agent count after dispatch: `1/2 implementation, 0/2 read-only, 1/4 total`
- Recovery from: `none`
- Recovered owner status: `n/a`

## Context
- `docs/codex/0.11.1-program.md`
- `docs/operations/LIVE_ACCEPTANCE_RUNBOOK.md`

## Verification
- `python -m pytest platform-api/tests/test_funding_local_closed_loop.py` (sandbox: 2 passed)
- ruff on the changed app files and test passes
- `git diff --stat` of the slice = 3 modified app files + 1 new app module + 1 new test + control docs

## Progress
- Done: `funding.py` funding market-command adapter (perpetual-first legs, order execution intents, live-execution gate) and `FundingMarketCommandRequest` schema.
- Done: `execution_batches` funding hedge hook `resize_funding_spot_hedge` releasing the spot leg only for the confirmed perpetual fill (proportional, never above the instruction) plus the funding trading route.
- Done: E2E closed-loop test: hedged open with both legs filled, positions, exact platform<->venue reconciliation, close returning both positions to zero, and runtime-unreachable fail-closed (manual_intervention, perpetual result_unknown, spot never released).
- Current: awaiting owner receipt review.
- Next: owner reviews receipt, then authorizes external read-only connection verification (待授权) or the final independent acceptance.
- Blocked by: none for this closure. DSH pre-existing-file write protection blocks in-place disk edits; evidence is committed via the git object pipeline.