# Compatibility Pointer

This file is retained temporarily so old links do not break. It is not a current-state authority and must not contain duplicated version, branch, CI, architecture or task-progress facts.

Read in this order:

1. `AGENTS.md` for durable repository rules;
2. `docs/codex/current-state.md` for the sole current engineering state;
3. `docs/codex/context-map.md` for bounded task-specific reading packs;
4. the nearest module `AGENTS.md`, directly affected source files and direct tests.

New documentation must link to `docs/codex/current-state.md`, not to this compatibility pointer. This file can be removed in a later cleanup phase after references are proven absent.
