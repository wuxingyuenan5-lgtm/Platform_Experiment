# Platform 0.11.1 Program Control

Status: authorized for governance updates and business implementation. External connections, credentials, Live Write and controlled-live operations require separate owner authorization.

This is the single bounded control entry for the active 0.11.1 version task. Keep source, full diffs, long logs and agent process out of this file. After final acceptance, merge durable facts into their owning authorities and retain this file only as a short historical index.

## Version objective

Complete two local CEO-operated strategy closures with verified real data, controlled execution and auditable records:

- Cross-venue spread: existing Bybit `XAUTUSDT` perpetual leg and Bybit-provided MT5 `XAUUSD+` gold leg.
- Funding carry: one Bybit account, CEO-specified perpetual-short plus spot-long pairs, supporting multiple concurrently held symbols.

Both strategies reuse one bounded two-leg command, execution-batch, idempotency, ledger, reconciliation and recovery foundation. They must record instructions, external orders, fills or deals, positions, fees, funding, PnL/NAV, operating state and reconciliation evidence.

## Approved product decisions

- Cross-spread remains limited to the existing gold pair for 0.11.1.
- Funding carry uses perpetual short plus spot long only; reverse carry and cross-venue funding carry are excluded.
- The CEO manually enters both symbols and both quantities and owns economic sizing. The platform does not select symbols, size, rebalance or silently alter quantities.
- Funding open and close execute the perpetual leg first with bounded PostOnly chase, then the spot leg only after authoritative first-leg disposition. It never silently falls back to market.
- Funding acceptance crosses at least one real funding settlement and reconciles the resulting external ledger fact.
- Multiple funding pairs may be held concurrently, while new open or close execution batches remain serialized.
- One CEO instruction creates at most one execution batch. Child amend or cancel-repost activity cannot increase cumulative fill beyond the instruction quantity. Duplicate submission, restart, timeout, disconnect or `result_unknown` cannot create a second intent.
- Partial-fill handling must preserve authoritative cumulative fill, stop unbounded chase and minimize naked exposure. The technical lead owns the exact proposal after reviewing current execution contracts and official Bybit behavior.
- A persistent MT5 connector owns one ordinary-account Terminal session. Browser and Platform API reads do not log in or switch MT5 accounts per request. Future accounts use isolated connector instances.
- Only the CEO may initiate strategy open, close or risk-disposition commands through the website. Browser identity does not bypass Live Write, Kill Switch, idempotency, reconciliation or Runtime gates.
- Existing frontend layout, visual hierarchy and interaction style are frozen by default. Changes are limited to necessary real-data wiring, state disclosure and approved trading controls; material redesign returns to the owner.
- 0.11.1 is local acceptance only. Server deployment, production operation and unattended trading are excluded.

## Authority and role routing

- Project owner: product scope, priority, external connection, each Live Write step, risk acceptance and final acceptance.
- Program orchestrator: maintains this control state, routes one version objective to the technical lead, receives receipts of at most 800 Chinese characters, and escalates owner decision gates and material uncertainty. It does not manage source or implementation agents.
- Project technical lead: owns technical investigation, architecture, task cards, Context Packs, single-writer boundaries, temporary execution channels, technical acceptance and evidence sufficiency.
- Development advisor: read-only independent challenge at scope expansion, material rework or Token anomaly, architecture dispute, controlled-live gate and final acceptance; not part of routine delivery.
- Temporary agents: created only when they reduce expected rework. Investigation is normally handled by the technical lead; each active code workflow has one implementer; critical immutable changes receive independent acceptance.
- Web GPT: preferred for bounded public-document research, test matrices, low-risk analysis and ordinary web-suitable implementation support. It cannot decide or perform credentials, external-account access, database migration authority, identity or permission authority, core trading side effects, Live Write or controlled-live operations.

## Active task card

- `role`: Project technical lead.
- `objective`: Organize and technically close Platform 0.11.1 as one version task under the objective and decisions above.
- `context_pack`: Start with `governance`; select task-type Packs for each bounded workflow, including `cross-spread-live`, `funding-data`, `strategy-management`, `identity-permission`, `trading-display`, `live-readiness` and `release-acceptance` only when that workflow starts.
- `authority`: Governance and business implementation are authorized. External connections, credentials, Live Write and real operations are not authorized.
- `write_scope`: Repository authorities, Platform API, Execution Runtime, database migrations, tests and only necessary frontend real-data or trading-control changes within approved task cards and module rules.
- `non_goals`: Production deployment; unattended trading; automatic symbol selection, sizing or rebalancing; reverse or cross-venue funding carry; other strategies; material frontend redesign.
- `acceptance`: Both strategies satisfy the unified completion definition below with immutable code and test evidence, then separately authorized external and controlled-live evidence.
- `stop_conditions`: Any request for credentials, external connection or Live Write without a fresh owner gate; safety-contract conflict; ambiguous external order identity; duplicate or over-quantity possibility; shared writer overlap; material frontend redesign; production scope expansion.
- `output_contract`: Return only `outcome`, `changed_files`, `validations`, `evidence`, `contract_impact`, `unproven_facts`, `residual_risks` and `next_gate`, with the complete receipt at most 800 Chinese characters.

## Current control state

- Current milestone: technical-lead investigation and integrated execution proposal.
- Active work: technical lead to map reuse of the existing two-leg engine, MT5 persistent connector, Bybit funding data and private-account facts, unified ledger, CEO-only command path, minimal frontend wiring, testing and acceptance route.
- Blockers: existing live runbook contains fixed amount, daily volume, batch-count and loss limits while the owner does not want a new funding amount cap. No Live Write may proceed until the technical lead provides a contract-safe proposal and the owner approves the resulting rule. The repository Context Pack budget check also has five pre-existing over-budget Packs; the technical lead must avoid those Packs unless needed and route bounded budget maintenance separately rather than expanding implementation context.
- Next authorization: none for repository investigation and implementation. Separate owner approval is required before first read-only external connection, credential configuration, every Live Write window and each controlled-live step.

## Escalation and traceability

- Stop and ask the owner before any material scope change, unresolved product assumption, safety-contract reinterpretation, architecture choice that changes future operating constraints, significant frontend behavior or visual change, external connection, credential use, Live Write or controlled-live disposition.
- The technical lead may resolve ordinary implementation details inside approved contracts, but may not infer product intent or risk acceptance from silence.
- Every implementation workflow uses a task card, a `codex/` branch or approved existing branch, reviewable commits, an immutable PR or commit range, Pack checks and a short closure receipt.
- Decisions belong in this control file or an owning authority; one-time implementation state and evidence belong in Issue or PR records. Do not rely on chat history, uncommitted local state or full agent transcripts for later recovery.
- Preserve public contracts and append-only migration discipline. Do not rewrite or squash away evidence needed to explain which version introduced a behavior unless the owner explicitly approves that Git operation.

## Unified completion definition

- Cross-spread completes local controlled open, close and failure handling for the approved gold pair with venue evidence and exact reconciliation.
- Funding carry completes local controlled open, at least one verified funding settlement, close and failure handling for multiple configurable Bybit symbol pairs, without automatic sizing or rebalancing.
- CEO-only commands are persisted before side effects and remain idempotent, bounded, auditable and recoverable.
- External orders, fills or deals, positions, fees, funding, PnL/NAV, data freshness, runtime state and reconciliation are queryable and traceable to authoritative facts.
- `result_unknown`, partial fill, disconnect, restart and identity mismatch fail closed and cannot silently retry, expand quantity or present unknown financial state as zero.
- Strategy management and market pages use verified real data and preserve the accepted frontend appearance except for necessary approved controls and states.
- Live Write returns to disabled after every authorized window. Independent acceptance passes. No production, deployment or unattended capability is claimed.

## Evidence index

- Scope approval: owner decision in the active Program Orchestrator task on 2026-08-13; durable scope is recorded in `docs/codex/current-state.md` and this control file.
- Technical proposal: pending technical-lead receipt.
- Advisor review: approved repository investigation and implementation on 2026-08-13, while keeping external connection and Live Write blocked. Mandatory proposal topics are limit-contract compatibility, quantity handling, partial-fill naked exposure, global batch serialization, restart recovery and funding-ledger evidence. The advisor warned against a generic strategy platform, automatic sizing, event-bus introduction or frontend redesign.
- Implementation PRs and acceptance evidence: pending.
