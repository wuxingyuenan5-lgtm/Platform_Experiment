# V6 交易安全加固实施计划

状态：`Phase 1 completed / Phase 2 completed / Phase 3 completed / Phase 4A acceptance pending`  
Phase 3 代码发布提交：`77bf4223c2059d5a56fc08a2d49214351c396abc`  
总跟踪 Issue：`#2 V6 main：交易安全、可靠执行与账务正确性落地计划`  
Phase 4 总计划：Issue `#12`  
Phase 4A：Issue `#14`、PR `#15`、`V6-Phase4A-执行风险与Kill-Switch.md`  
更新时间：`2026-07-23`

`main` 的最新分支指针以 GitHub 为准；本计划记录阶段代码发布提交，不硬编码会被后续文档提交推动的 main tip。

## 1. 总目标

在不扩张新策略、新页面和复杂基础设施的前提下，依次建立：

```text
交易输入可验证
+
命令和执行幂等
+
结果未知可恢复
+
金融事实可重建
+
双腿风险可处置
+
Demo 执行可查询
+
日终差异可审计
+
代码、测试、文档和 CI 全程留痕
```

完成 Phase 4 全部验收和独立 Live 发布评审前，不开放真实资金 Live。

## 2. 总体路线

| 阶段 | 目标 | 状态 | 发布门禁 |
|---|---|---|---|
| Phase 1 | Fail-closed、Runtime 原子抢占、CI 与文档治理 | 已完成并合并 | 仅 Simulation / Fake |
| Phase 2 | TradeCommand 正式入口、`result_unknown` 恢复、动态 Catalog | 已完成并合并 | 仍仅 Simulation / Fake |
| Phase 3 | 不可变事实、合约规格、正式 PnL 与统一估值 NAV | 已完成并合并 | PnL/NAV 可用于内部核对 |
| Phase 4A | Kill Switch、腿间延迟、残留敞口、幂等处置 | 实现完成，等待最终验收 | 仍仅 Simulation / Fake |
| Phase 4B | 外部 Venue 主动查询、事实导入、差异对象 | 待实施 | 只允许受控查询／模拟 |
| Phase 4C | Bybit Demo 与 MT5 Demo 闭环 | 待实施 | Demo 与 Simulation 隔离 |
| Phase 4D | 日终对账、故障演练和连续运行验收 | 待实施 | 完成后才讨论 Live 评审 |

## 3. Phase 1：交易安全底线

已完成：

- 未知账户、停用账户、未知标的和缺失 ContractSpecification 全部 fail-closed。
- 数量满足最小下单量和数量步长，限价满足价格步长。
- 凭证只保存 `secret://...` 引用。
- Live 受全局 `live_trading_enabled=false` 门禁保护。
- Runtime 原子抢占 command，重复 command 不产生第二次外部副作用。
- Backend、Runtime、Frontend 的测试、类型检查和生产构建纳入 CI。

验收：`Platform CI run 29983926790`，全部通过。

## 4. Phase 2：命令入口与结果恢复

已完成：

1. 单腿交易统一通过 `TradeCommand + idempotencyKey`。
2. ExecutionBatch 每条腿生成独立 TradeCommand。
3. Batch 强制提供 `strategyInstanceId` 与 `idempotencyKey`。
4. TradeCommand 校验 Strategy、Account Binding、Account、Instrument 和 ContractSpecification。
5. `/trading/orders` 只保留为 deprecated 兼容入口。
6. `result_unknown` 从 Runtime Journal 恢复，不重新下单。
7. Fill event 重放不会重复更新 Position 和 PnL。
8. 前端从 Backend Catalog 动态获取策略、账户、绑定与标的。
9. 同一幂等键复用不同载荷时返回 409。
10. 权威 Markdown 变化纳入 CI 触发范围。

验收：`Platform CI run 29986397987`，Backend、Runtime、Frontend 全部通过。

详细记录：`V6-Phase2-命令入口与结果恢复.md`。

## 5. Phase 3：金融事实与正式账务

已完成，代码发布提交为 `77bf4223c2059d5a56fc08a2d49214351c396abc`：

1. 新增不可变 FinancialFact，支持 Order、Fill／Deal、Funding、Swap、Fee、Balance、Position 和 FX。
2. 客户端幂等键与外部事实身份双重去重；相同身份不同载荷返回 409。
3. Quantity Unit、Settlement Currency 和 Contract Multiplier 由后端 Catalog 快照确定。
4. Formal Position、Formal PnL 可以从事实完整重建。
5. PnL 分项保存 Trading、Funding、Swap、Fee、FX 和 Total。
6. 非基础币种缺失 FX 时标记 incomplete，不按 1:1 静默换算。
7. Formal NAV 对全部 active binding 账户使用同一 valuationTime。
8. NAV 返回账户覆盖数和 missingAccountIds，不把缺失账户当作零。
9. 已加入资费套利 PnL 与多账户 NAV 金样本。
10. 事实入库、投影重建和 NAV 创建均写入 AuditEvent。

验收：`Platform CI #127 / run 29993137286`，Backend、Runtime、Frontend 全部通过。

Phase 3 完成只代表 Formal PnL/NAV 可以用于内部核对，不代表可以实盘。

## 6. Phase 4A：执行风险控制

已实现，等待最终 CI 和 PR 验收：

1. Global、Strategy、Account 三级 Kill Switch。
2. Kill Switch 写入使用 idempotencyKey 和载荷冲突检测。
3. Batch 认领前与每条 Leg 执行前双重检查。
4. 每个 StrategyInstance 配置最大腿间延迟、最大残留名义敞口和失败处置策略。
5. 每个 Batch 固化风险策略快照和独立风险状态。
6. 残留敞口使用 Fill、Contract Multiplier 和 Settlement Currency。
7. 支持 hold_and_escalate、flatten_filled_legs、cancel_open_legs、substitute_hedge。
8. 自动平仓通过反向 TradeCommand，不绕过命令层。
9. Kill Switch、策略变更、风险状态和风险动作全部写入 AuditEvent。
10. 已加入 Kill Switch、残留超限、自动平仓、动作幂等和腿间超时金样本。

实施记录：

- `V6-Phase4A-执行风险与Kill-Switch.md`
- `../technical/EXECUTION_RISK_CONTROLS.md`
- Issue `#14`
- PR `#15`

## 7. 当前明确限制

- 只有 Fake Gateway 具备完整下单闭环。
- Bybit/MT5 尚未形成真实 Demo 下单、撤单、查单和事实自动导入链路。
- `result_unknown` 当前只从 Runtime Journal 恢复，尚未主动查询外部 Venue。
- 已存在外部 Order 的撤单仍等待 Phase 4B/4C Gateway 能力。
- 多币种残留敞口在没有风险 FX 快照时使用保守 MIXED／incomplete 口径。
- 外部账单解析和逐笔账单核对尚未完成。
- 应用层认证、RBAC、双人审批和生产密钥托管尚未落地。

因此，禁止将当前版本解释为可实盘版本。

## 8. Phase 4B–4D 入口

### Phase 4B：外部查询与事实导入

1. Runtime 统一查询 Order、Fill／Deal、Position、Balance。
2. `result_unknown` 主动查询外部 Venue。
3. 外部结果规范化为 FinancialFact。
4. 建立 Reconciliation Difference，不无痕覆盖冲突。

### Phase 4C：Demo 执行

1. Bybit Demo 下单、撤单、查单、成交、持仓、余额和 Funding。
2. MT5 Demo Order／Deal、持仓、余额和 Swap。
3. Demo、Simulation 和 Live 凭证与账户隔离。

### Phase 4D：日终对账

1. Order、Fill／Deal、Position、Balance、Funding、Swap、Fee 对账。
2. Formal Position、PnL 和 NAV 对账。
3. 断网、超时、Runtime 重启和单腿失败演练。
4. 连续运行和零未解释差异验收。

## 9. 工程治理规则

每个阶段必须同时完成：

- 独立 Issue、分支和 Pull Request。
- 代码与自动测试。
- CI 通过记录。
- 对应实施计划和技术设计。
- README、START-HERE、Release Gate、API Spec 和 Changelog 更新。
- 明确未完成范围、发布门禁和回滚方式。

不允许通过直接强推 main 绕过 PR 和 CI。