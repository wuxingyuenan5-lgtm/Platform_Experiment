# 跨所黄金价差

## 目标

围绕 Bybit `XAUTUSDT` 与 MT5 `XAUUSD+` 的既定黄金组合，完成研究、指令、两腿受控执行、配平、敞口、订单成交、费用、PnL、NAV、恢复和对账闭环。

相似的黄金 Symbol 不代表相同合约。数量换算必须显式使用各 Venue 的合约乘数、最小数量、步长、tick、币种、交易时段和费用语义。

## 当前业务合同

- Owner 明确指定方向和业务数量；系统不自动扩大标的或规模。
- 一个业务指令最多创建一个 ExecutionBatch，两腿共享稳定的关联身份。
- Platform API 负责编排、风险、账本和对账；Venue SDK 与订单副作用只在 Execution Runtime。
- ACK 不等于 Fill；成交事实来自标准化外部结果和恢复查询。
- 每条腿的累计成交、换算后等价数量和剩余暴露必须独立记录。

## 执行与恢复

- 重复提交、并发、重启和断线不能创建第二个业务意图。
- 修改、撤单重挂和恢复动作不得突破原指令累计成交上限。
- 任一腿结果未知时进入 `result_unknown`，停止新副作用并执行查询、对账或人工接管。
- 部分成交需要显式展示已配平量、未配平量和当前风险，不能用批次最终状态掩盖腿级事实。
- Fill、Deal、Commission、Swap、Funding、FX 和合约换算必须保持来源与时间语义。

## 双边资金划转

- “移动双边资金”必须从平台内真实完成 `bybit-live-main` 与其 MT5/TradFi 账户之间的资金划转；跳转 Bybit 官网由 Owner 手工操作不属于该功能验收通过。
- 自动划转只能接入 Bybit 已验证、可审计且适合服务器部署的正式接口。MT5 Python Terminal API 不负责托管账户入金；不得用浏览器自动化、未公开网页接口或凭据推断冒充生产集成。
- 在正式接口未验证前，平台不得把辅助记录、余额预约或官网链接呈现为可用的自动划转能力。已有辅助记录只作为未完成实验处理，不形成产品能力。
- 正式接入后，新请求必须保存稳定业务身份与源/目标逻辑账户；只有权威转账身份和双边余额共同确认时才完成并释放账户级 claim 与余额预约。结果未知、查询不可用或单边变化继续 fail closed。
- 逻辑 UTA 与 MT5/TradFi 账户必须显式绑定，不得按展示名或凭据相同推断。Bybit 当前公开 V5 Internal Transfer 写合同只接受 `UNIFIED` 与 `FUND`，向 `TradFi` 写入会返回 `131203`；API Key 的 `AccountTransfer` 权限不能扩展该合同。官方网页实际使用 `/v3/private/asset/transfer/mt5/precheck|deposit|withdraw` 专用合同，但该合同依赖网页登录 token，现有 API Key 以 V5 或旧式 HMAC 签名访问均返回 `10007 User authentication failed`。余额查询或 Asset Overview 能识别 TradFi 不等于 API Key 具备写入能力，平台必须在移动 UTA 资金前保持不可提交。
- 只有 Bybit 为 MT5 CFD 专用合同提供可长期部署的 API Key、OAuth 扩展范围或机构授权，且真实账户 readiness 与最小写入验收均通过后，才能启用平台内自动划转。网页登录会话可以完成 Owner 监督下的单次入金，但不能作为服务器长期凭据，也不构成第三模板自动化验收。届时创建请求必须使用稳定业务身份；创建响应不视为结算完成，`PENDING`、查询不可用和未知状态保留 claim 与余额预约，权威终态后才释放。

## 明确不包含

- 自动寻找更多黄金 Venue 或 Symbol。
- 自动决定仓位、阈值或扩大资金规模。
- 海内外价差、CTP 或国内期货执行。
- 无人值守 Live 交易。
- 用 Fake Gateway 或样例数据证明真实外部闭环。

## 完成证据

- Bybit 与 MT5 的标的身份、合约规格和数量换算经过核实。
- 两腿订单、成交、费用、持仓和外部身份可以稳定关联。
- 断线、重复、乱序、部分成交和结果未知场景 fail closed。
- 关闭后残余敞口、费用、PnL 和账户事实完成对账。
- 每个外部连接或真实写入步骤均有单独 Owner 授权；本地验收不能替代外部证据。
