name: Hedge Board Browser E2E

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - feature/issue-134-platform-0-9-1-unified-delivery
    paths:
      - '.github/workflows/hedge-board-e2e.yml'
      - 'admin-risk/.env.development'
      - 'admin-risk/.env.production'
      - 'admin-risk/package.json'
      - 'admin-risk/pnpm-lock.yaml'
      - 'admin-risk/playwright.hedge-board.config.ts'
      - 'admin-risk/e2e/hedge-board/**'
      - 'admin-risk/scripts/verify-hedge-board-layout.cjs'
      - 'admin-risk/src/api/hedgeResearch.ts'
      - 'admin-risk/src/api/platform/researchWatchlist.ts'
      - 'admin-risk/src/api/platform/userSystem.ts'
      - 'admin-risk/src/router/routes/modules/hedge.ts'
      - 'admin-risk/src/store/modules/user.ts'
      - 'admin-risk/src/views/hedgeBoard/**'
      - 'docs/operations/HEDGE_BOARD_ACCEPTANCE.md'
      - 'docs/operations/PLATFORM_0_9_1_LOCAL_ACCEPTANCE_HANDOFF.md'
      - 'tasks/issue-134-platform-0-9-1-unified-delivery.md'
      - 'platform-backend/app/auth.py'
      - 'platform-backend/app/config.py'
      - 'platform-backend/app/main.py'
      - 'platform-backend/app/research_*.py'
      - 'platform-backend/app/schema_migrations.py'
      - 'platform-backend/app/user_*.py'
      - 'platform-backend/scripts/seed_hedge_board_e2e.py'
      - 'platform-backend/pyproject.toml'

permissions:
  contents: read

concurrency:
  group: hedge-board-e2e-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  browser-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      CI: 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: platform-backend/pyproject.toml

      - name: Install Platform Backend
        run: python -m pip install -e './platform-backend[dev]'

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm
          cache-dependency-path: admin-risk/pnpm-lock.yaml

      - name: Install frontend dependencies
        working-directory: admin-risk
        env:
          HUSKY: '0'
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        working-directory: admin-risk
        run: pnpm exec playwright install --with-deps chromium

      - name: Generate isolated employee credentials
        shell: bash
        run: |
          python - <<'PY'
          import os
          import secrets

          with open(os.environ['GITHUB_ENV'], 'a', encoding='utf-8') as output:
              output.write('E2E_CEO_USERNAME=e2e_employee_1\n')
              output.write(f"E2E_CEO_PASSWORD=Cc9!{secrets.token_urlsafe(24)}\n")
          PY

      - name: Run hedge-board browser acceptance
        working-directory: admin-risk
        run: pnpm test:e2e:hedge-board

      - name: Upload browser diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hedge-board-browser-e2e
          path: |
            admin-risk/playwright-report/hedge-board
            admin-risk/test-results/hedge-board
          if-no-files-found: warn
          retention-days: 3
