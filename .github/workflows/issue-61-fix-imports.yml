name: Issue 61 Import Fix

on:
  push:
    branches:
      - refactor/issue-61-shared-position-math

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/issue-61-shared-position-math
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Ruff import ordering fixes
        run: python scripts/issue-61-fix-imports.py
      - name: Commit patched files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix Position Math import ordering"
          git push origin HEAD:refactor/issue-61-shared-position-math
