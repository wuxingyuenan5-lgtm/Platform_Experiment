name: Apply EOD Service Refactor

on:
  push:
    branches:
      - refactor/issue-81-eod-reconciliation-service
    paths:
      - scripts/apply_eod_service_refactor.py
      - .github/workflows/apply-eod-service-refactor.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/issue-81-eod-reconciliation-service
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Apply bounded Service and ownership updates
        run: python scripts/apply_eod_service_refactor.py
      - name: Validate patch
        run: git diff --check
      - name: Commit generated updates
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No generated updates required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            platform-backend/app/eod_reconciliation_service.py \
            docs/architecture/OWNERSHIP.md \
            docs/codex/current-state.md \
            docs/engineering/TECHNICAL_DEBT.md \
            scripts/check-documentation-consistency.py \
            CHANGELOG.md
          git commit -m "Synchronize EOD service ownership"
          git push origin HEAD:refactor/issue-81-eod-reconciliation-service
