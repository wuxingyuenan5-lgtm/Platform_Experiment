name: Issue 63 One-Time Patch

on:
  push:
    branches:
      - refactor/issue-63-order-submission-orchestration

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/issue-63-order-submission-orchestration
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply unified order-submission patch
        run: python scripts/issue-63-apply.py
      - name: Commit patched files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Unify Platform order submission orchestration"
          git push origin HEAD:refactor/issue-63-order-submission-orchestration
