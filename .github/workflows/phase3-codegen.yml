name: Phase 3 Codegen

on:
  push:
    branches:
      - hardening/v6-financial-facts-phase3
    paths:
      - '.github/workflows/phase3-codegen.yml'

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hardening/v6-financial-facts-phase3
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Phase 3 implementation
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file = Path(path)
              text = file.read_text(encoding='utf-8')
              if old not in text:
                  raise RuntimeError(f'marker not found in {path}: {old[:80]!r}')
              file.write_text(text.replace(old, new, 1), encoding='utf-8')

          def append_once(path: str, marker: str, content: str) -> None:
              file = Path(path)
              text = file.read_text(encoding='utf-8')
              if marker in text:
                  return
              file.write_text(text.rstrip() + '\n\n' + content.strip() + '\n', encoding='utf-8')

          database_tables = r'''
          CREATE TABLE IF NOT EXISTS financial_facts (
              id TEXT PRIMARY KEY,
              idempotency_key TEXT NOT NULL UNIQUE,
              content_hash TEXT NOT NULL,
              fact_type TEXT NOT NULL,
              source TEXT NOT NULL,
              external_id TEXT NOT NULL,
              strategy_instance_id TEXT NOT NULL,
              account_id TEXT,
              instrument_id TEXT,
              side TEXT,
              quantity TEXT,
              quantity_unit TEXT,
              price TEXT,
              contract_multiplier TEXT,
              amount TEXT,
              currency TEXT,
              base_currency TEXT NOT NULL,
              fx_rate_to_base TEXT,
              converted_amount TEXT,
              available_balance TEXT,
              occurred_at TEXT NOT NULL,
              payload_json TEXT NOT NULL,
              data_quality_state TEXT NOT NULL,
              created_at TEXT NOT NULL,
              UNIQUE(source, external_id, fact_type),
              FOREIGN KEY(strategy_instance_id) REFERENCES strategy_instances(id),
              FOREIGN KEY(account_id) REFERENCES accounts(id),
              FOREIGN KEY(instrument_id) REFERENCES instruments(id)
          );

          CREATE TABLE IF NOT EXISTS formal_positions (
              strategy_instance_id TEXT NOT NULL,
              account_id TEXT NOT NULL,
              instrument_id TEXT NOT NULL,
              net_quantity TEXT NOT NULL,
              average_price TEXT,
              quantity_unit TEXT NOT NULL,
              data_quality_state TEXT NOT NULL,
              updated_at TEXT NOT NULL,
              PRIMARY KEY(strategy_instance_id, account_id, instrument_id),
              FOREIGN KEY(strategy_instance_id) REFERENCES strategy_instances(id),
              FOREIGN KEY(account_id) REFERENCES accounts(id),
              FOREIGN KEY(instrument_id) REFERENCES instruments(id)
          );

          CREATE TABLE IF NOT EXISTS formal_pnl_results (
              strategy_instance_id TEXT NOT NULL,
              account_id TEXT NOT NULL,
              instrument_id TEXT NOT NULL,
              currency TEXT NOT NULL,
              trading_pnl TEXT NOT NULL,
              funding_pnl TEXT NOT NULL,
              swap_pnl TEXT NOT NULL,
              fee_pnl TEXT NOT NULL,
              fx_pnl TEXT NOT NULL,
              total_pnl TEXT NOT NULL,
              fact_count INTEGER NOT NULL,
              data_quality_state TEXT NOT NULL,
              updated_at TEXT NOT NULL,
              PRIMARY KEY(strategy_instance_id, account_id, instrument_id),
              FOREIGN KEY(strategy_instance_id) REFERENCES strategy_instances(id),
              FOREIGN KEY(account_id) REFERENCES accounts(id),
              FOREIGN KEY(instrument_id) REFERENCES instruments(id)
          );

          CREATE TABLE IF NOT EXISTS formal_strategy_nav_snapshots (
              id TEXT PRIMARY KEY,
              strategy_instance_id TEXT NOT NULL,
              valuation_time TEXT NOT NULL,
              equity TEXT,
              capital_base TEXT NOT NULL,
              nav TEXT,
              currency TEXT NOT NULL,
              data_quality_state TEXT NOT NULL,
              required_account_count INTEGER NOT NULL,
              included_account_count INTEGER NOT NULL,
              missing_account_ids_json TEXT NOT NULL,
              created_at TEXT NOT NULL,
              FOREIGN KEY(strategy_instance_id) REFERENCES strategy_instances(id)
          );

          CREATE INDEX IF NOT EXISTS idx_financial_facts_strategy_time
          ON financial_facts(strategy_instance_id, occurred_at);

          CREATE INDEX IF NOT EXISTS idx_financial_facts_account_instrument
          ON financial_facts(account_id, instrument_id, occurred_at);

          CREATE INDEX IF NOT EXISTS idx_formal_nav_strategy_time
          ON formal_strategy_nav_snapshots(strategy_instance_id, valuation_time);

          '''
          replace_once(
              'platform-backend/app/database.py',
              'CREATE TABLE IF NOT EXISTS market_spread_snapshots (',
              database_tables + 'CREATE TABLE IF NOT EXISTS market_spread_snapshots (',
          )

          schema_models = r'''
          FinancialFactType = Literal[
              "external_order",
              "trade_fill",
              "deal",
              "funding",
              "swap",
              "fee",
              "balance",
              "position",
              "fx",
          ]


          class CreateFinancialFactRequest(BaseModel):
              idempotency_key: str = Field(alias="idempotencyKey", min_length=1, max_length=128)
              fact_type: FinancialFactType = Field(alias="factType")
              source: str = Field(min_length=1, max_length=64)
              external_id: str = Field(alias="externalId", min_length=1, max_length=128)
              strategy_instance_id: str = Field(alias="strategyInstanceId")
              account_id: str | None = Field(default=None, alias="accountId")
              instrument_id: str | None = Field(default=None, alias="instrumentId")
              side: Literal["buy", "sell"] | None = None
              quantity: Decimal | None = Field(default=None, gt=0)
              price: Decimal | None = Field(default=None, gt=0)
              amount: Decimal | None = None
              currency: str | None = Field(default=None, min_length=3, max_length=16)
              available_balance: Decimal | None = Field(default=None, alias="availableBalance")
              fx_rate_to_base: Decimal | None = Field(default=None, alias="fxRateToBase", gt=0)
              occurred_at: datetime = Field(alias="occurredAt")
              payload: dict[str, object] = Field(default_factory=dict)

              @model_validator(mode="after")
              def validate_fact_shape(self) -> "CreateFinancialFactRequest":
                  if self.fact_type in {"trade_fill", "deal"}:
                      if not all(
                          [self.account_id, self.instrument_id, self.side, self.quantity, self.price]
                      ):
                          raise ValueError("Trade facts require account, instrument, side, quantity and price")
                  elif self.fact_type in {"funding", "swap", "fee", "fx"}:
                      if self.account_id is None or self.instrument_id is None:
                          raise ValueError("PnL component facts require account and instrument")
                      if self.amount is None or self.currency is None:
                          raise ValueError("PnL component facts require amount and currency")
                  elif self.fact_type == "balance":
                      if self.account_id is None or self.amount is None or self.currency is None:
                          raise ValueError("Balance facts require account, amount and currency")
                  elif self.fact_type in {"external_order", "position"}:
                      if self.account_id is None or self.instrument_id is None:
                          raise ValueError("Order and position facts require account and instrument")
                  return self


          class FinancialFactResponse(BaseModel):
              fact_id: str = Field(alias="factId")
              idempotency_key: str = Field(alias="idempotencyKey")
              fact_type: FinancialFactType = Field(alias="factType")
              source: str
              external_id: str = Field(alias="externalId")
              strategy_instance_id: str = Field(alias="strategyInstanceId")
              account_id: str | None = Field(default=None, alias="accountId")
              instrument_id: str | None = Field(default=None, alias="instrumentId")
              side: str | None = None
              quantity: Decimal | None = None
              quantity_unit: str | None = Field(default=None, alias="quantityUnit")
              price: Decimal | None = None
              contract_multiplier: Decimal | None = Field(default=None, alias="contractMultiplier")
              amount: Decimal | None = None
              currency: str | None = None
              base_currency: str = Field(alias="baseCurrency")
              fx_rate_to_base: Decimal | None = Field(default=None, alias="fxRateToBase")
              converted_amount: Decimal | None = Field(default=None, alias="convertedAmount")
              available_balance: Decimal | None = Field(default=None, alias="availableBalance")
              occurred_at: datetime = Field(alias="occurredAt")
              data_quality_state: str = Field(alias="dataQualityState")
              created_at: datetime = Field(alias="createdAt")


          class FormalPositionResponse(BaseModel):
              strategy_instance_id: str = Field(alias="strategyInstanceId")
              account_id: str = Field(alias="accountId")
              instrument_id: str = Field(alias="instrumentId")
              net_quantity: Decimal = Field(alias="netQuantity")
              average_price: Decimal | None = Field(default=None, alias="averagePrice")
              quantity_unit: str = Field(alias="quantityUnit")
              data_quality_state: str = Field(alias="dataQualityState")
              updated_at: datetime = Field(alias="updatedAt")


          class FormalPnlResponse(BaseModel):
              strategy_instance_id: str = Field(alias="strategyInstanceId")
              account_id: str = Field(alias="accountId")
              instrument_id: str = Field(alias="instrumentId")
              currency: str
              trading_pnl: Decimal = Field(alias="tradingPnl")
              funding_pnl: Decimal = Field(alias="fundingPnl")
              swap_pnl: Decimal = Field(alias="swapPnl")
              fee_pnl: Decimal = Field(alias="feePnl")
              fx_pnl: Decimal = Field(alias="fxPnl")
              total_pnl: Decimal = Field(alias="totalPnl")
              fact_count: int = Field(alias="factCount")
              data_quality_state: str = Field(alias="dataQualityState")
              updated_at: datetime = Field(alias="updatedAt")


          class FormalNavSnapshotResponse(BaseModel):
              snapshot_id: str = Field(alias="snapshotId")
              strategy_instance_id: str = Field(alias="strategyInstanceId")
              valuation_time: datetime = Field(alias="valuationTime")
              equity: Decimal | None = None
              capital_base: Decimal = Field(alias="capitalBase")
              nav: Decimal | None = None
              currency: str
              data_quality_state: str = Field(alias="dataQualityState")
              required_account_count: int = Field(alias="requiredAccountCount")
              included_account_count: int = Field(alias="includedAccountCount")
              missing_account_ids: list[str] = Field(alias="missingAccountIds")
              created_at: datetime = Field(alias="createdAt")


          class FinancialProjectionRebuildResponse(BaseModel):
              strategy_instance_id: str = Field(alias="strategyInstanceId")
              rebuilt_pair_count: int = Field(alias="rebuiltPairCount")
              fact_count: int = Field(alias="factCount")
              completed_at: datetime = Field(alias="completedAt")
          '''
          append_once('platform-backend/app/schemas.py', 'class CreateFinancialFactRequest', schema_models)

          financial_facts_module = r'''from __future__ import annotations

import hashlib
import json
from datetime import UTC, datetime
from decimal import Decimal
from uuid import uuid4

from fastapi import HTTPException

from app.database import connection
from app.schemas import (
    CreateFinancialFactRequest,
    FinancialFactResponse,
    FinancialProjectionRebuildResponse,
    FormalNavSnapshotResponse,
    FormalPnlResponse,
    FormalPositionResponse,
)

PROJECTED_FACT_TYPES = {"trade_fill", "deal", "funding", "swap", "fee", "fx"}
TRADE_FACT_TYPES = {"trade_fill", "deal"}
MONETARY_FACT_TYPES = {"funding", "swap", "fee", "fx", "balance"}


def now_iso() -> str:
    return datetime.now(UTC).isoformat()


def decimal_text(value: Decimal) -> str:
    return format(value, "f")


def optional_decimal(value: object) -> Decimal | None:
    return Decimal(str(value)) if value is not None else None


def utc_iso(value: datetime | None) -> str:
    moment = value or datetime.now(UTC)
    if moment.tzinfo is None:
        moment = moment.replace(tzinfo=UTC)
    return moment.astimezone(UTC).isoformat()


def record_financial_fact(request: CreateFinancialFactRequest) -> FinancialFactResponse:
    normalized = normalize_fact(request)
    content_hash = hashlib.sha256(
        json.dumps(normalized, sort_keys=True, separators=(",", ":")).encode("utf-8")
    ).hexdigest()

    with connection() as db:
        existing = db.execute(
            """
            SELECT * FROM financial_facts
            WHERE idempotency_key = ?
               OR (source = ? AND external_id = ? AND fact_type = ?)
            ORDER BY created_at
            LIMIT 1
            """,
            (
                request.idempotency_key,
                request.source,
                request.external_id,
                request.fact_type,
            ),
        ).fetchone()
        if existing is not None:
            if existing["content_hash"] != content_hash:
                raise HTTPException(
                    status_code=409,
                    detail="Financial fact identity was reused with a different payload",
                )
            return financial_fact_from_row(existing)

        fact_id = str(uuid4())
        created_at = now_iso()
        cursor = db.execute(
            """
            INSERT OR IGNORE INTO financial_facts (
                id, idempotency_key, content_hash, fact_type, source, external_id,
                strategy_instance_id, account_id, instrument_id, side, quantity,
                quantity_unit, price, contract_multiplier, amount, currency,
                base_currency, fx_rate_to_base, converted_amount, available_balance,
                occurred_at, payload_json, data_quality_state, created_at
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                fact_id,
                request.idempotency_key,
                content_hash,
                normalized["fact_type"],
                normalized["source"],
                normalized["external_id"],
                normalized["strategy_instance_id"],
                normalized["account_id"],
                normalized["instrument_id"],
                normalized["side"],
                normalized["quantity"],
                normalized["quantity_unit"],
                normalized["price"],
                normalized["contract_multiplier"],
                normalized["amount"],
                normalized["currency"],
                normalized["base_currency"],
                normalized["fx_rate_to_base"],
                normalized["converted_amount"],
                normalized["available_balance"],
                normalized["occurred_at"],
                normalized["payload_json"],
                normalized["data_quality_state"],
                created_at,
            ),
        )
        if cursor.rowcount == 0:
            existing = db.execute(
                """
                SELECT * FROM financial_facts
                WHERE idempotency_key = ?
                   OR (source = ? AND external_id = ? AND fact_type = ?)
                LIMIT 1
                """,
                (
                    request.idempotency_key,
                    request.source,
                    request.external_id,
                    request.fact_type,
                ),
            ).fetchone()
            if existing is None or existing["content_hash"] != content_hash:
                raise HTTPException(status_code=409, detail="Financial fact claim failed")
            return financial_fact_from_row(existing)
        db.execute(
            """
            INSERT INTO audit_events (
                id, event_type, subject_type, subject_id, details_json, created_at
            ) VALUES (?, ?, ?, ?, ?, ?)
            """,
            (
                str(uuid4()),
                "financial_fact_recorded",
                "financial_fact",
                fact_id,
                json.dumps(
                    {
                        "factType": request.fact_type,
                        "strategyInstanceId": request.strategy_instance_id,
                        "source": request.source,
                        "externalId": request.external_id,
                    },
                    ensure_ascii=False,
                    sort_keys=True,
                ),
                created_at,
            ),
        )
        row = db.execute("SELECT * FROM financial_facts WHERE id = ?", (fact_id,)).fetchone()

    if request.fact_type in PROJECTED_FACT_TYPES:
        rebuild_account_instrument_projection(
            request.strategy_instance_id,
            request.account_id or "",
            request.instrument_id or "",
        )
    return financial_fact_from_row(row)


def normalize_fact(request: CreateFinancialFactRequest) -> dict[str, str | None]:
    strategy = load_strategy(request.strategy_instance_id)
    base_currency = strategy["base_currency"]
    instrument = None

    if request.account_id is not None:
        validate_account_binding(request.strategy_instance_id, request.account_id)
    if request.instrument_id is not None:
        instrument = load_instrument(request.instrument_id)

    currency = request.currency.upper() if request.currency else None
    quantity_unit = None
    contract_multiplier = None
    if request.fact_type in TRADE_FACT_TYPES:
        if instrument is None:
            raise HTTPException(status_code=422, detail="Trade fact instrument is unavailable")
        settle_currency = instrument["settle_currency"]
        if currency is not None and currency != settle_currency:
            raise HTTPException(
                status_code=422,
                detail="Trade fact currency must match the instrument settlement currency",
            )
        currency = settle_currency
        quantity_unit = instrument["quantity_unit"]
        contract_multiplier = Decimal(instrument["contract_multiplier"])

    amount = request.amount
    fx_rate = request.fx_rate_to_base
    converted_amount = None
    data_quality_state = "complete"
    if request.fact_type in MONETARY_FACT_TYPES:
        if amount is None or currency is None:
            raise HTTPException(status_code=422, detail="Monetary fact is incomplete")
        if currency == base_currency:
            fx_rate = Decimal("1")
            converted_amount = amount
        elif fx_rate is not None:
            converted_amount = amount * fx_rate
        else:
            data_quality_state = "incomplete"
    elif request.fact_type in TRADE_FACT_TYPES and currency != base_currency and fx_rate is None:
        data_quality_state = "incomplete"

    return {
        "fact_type": request.fact_type,
        "source": request.source,
        "external_id": request.external_id,
        "strategy_instance_id": request.strategy_instance_id,
        "account_id": request.account_id,
        "instrument_id": request.instrument_id,
        "side": request.side,
        "quantity": decimal_text(request.quantity) if request.quantity is not None else None,
        "quantity_unit": quantity_unit,
        "price": decimal_text(request.price) if request.price is not None else None,
        "contract_multiplier": (
            decimal_text(contract_multiplier) if contract_multiplier is not None else None
        ),
        "amount": decimal_text(amount) if amount is not None else None,
        "currency": currency,
        "base_currency": base_currency,
        "fx_rate_to_base": decimal_text(fx_rate) if fx_rate is not None else None,
        "converted_amount": (
            decimal_text(converted_amount) if converted_amount is not None else None
        ),
        "available_balance": (
            decimal_text(request.available_balance)
            if request.available_balance is not None
            else None
        ),
        "occurred_at": utc_iso(request.occurred_at),
        "payload_json": json.dumps(request.payload, ensure_ascii=False, sort_keys=True),
        "data_quality_state": data_quality_state,
    }


def load_strategy(strategy_instance_id: str):
    with connection() as db:
        row = db.execute(
            """
            SELECT si.id, si.status, si.base_currency, si.capital_base, sd.v1_scope
            FROM strategy_instances si
            JOIN strategy_definitions sd ON sd.id = si.strategy_definition_id
            WHERE si.id = ?
            """,
            (strategy_instance_id,),
        ).fetchone()
    if row is None:
        raise HTTPException(status_code=404, detail="Strategy instance not found")
    if row["status"] != "active" or row["v1_scope"] != "closed_loop":
        raise HTTPException(status_code=422, detail="Strategy instance is not active closed-loop")
    return row


def validate_account_binding(strategy_instance_id: str, account_id: str) -> None:
    with connection() as db:
        row = db.execute(
            """
            SELECT sab.id
            FROM strategy_account_bindings sab
            JOIN accounts a ON a.id = sab.account_id
            WHERE sab.strategy_instance_id = ?
              AND sab.account_id = ?
              AND sab.status = 'active'
              AND a.status = 'active'
            """,
            (strategy_instance_id, account_id),
        ).fetchone()
    if row is None:
        raise HTTPException(status_code=403, detail="Account is not actively bound to strategy")


def load_instrument(instrument_id: str):
    with connection() as db:
        row = db.execute(
            """
            SELECT i.id, i.settle_currency, i.quantity_unit, cs.contract_multiplier
            FROM instruments i
            JOIN contract_specifications cs ON cs.instrument_id = i.id
            WHERE i.id = ?
            ORDER BY cs.effective_from DESC
            LIMIT 1
            """,
            (instrument_id,),
        ).fetchone()
    if row is None:
        raise HTTPException(
            status_code=422,
            detail="Instrument or contract specification is unavailable",
        )
    return row


def list_financial_facts(
    strategy_instance_id: str | None = None,
    fact_type: str | None = None,
    limit: int = 200,
) -> list[FinancialFactResponse]:
    clauses: list[str] = []
    parameters: list[object] = []
    if strategy_instance_id is not None:
        clauses.append("strategy_instance_id = ?")
        parameters.append(strategy_instance_id)
    if fact_type is not None:
        clauses.append("fact_type = ?")
        parameters.append(fact_type)
    where = f"WHERE {' AND '.join(clauses)}" if clauses else ""
    parameters.append(limit)
    with connection() as db:
        rows = db.execute(
            f"""
            SELECT * FROM financial_facts
            {where}
            ORDER BY occurred_at DESC, created_at DESC
            LIMIT ?
            """,
            tuple(parameters),
        ).fetchall()
    return [financial_fact_from_row(row) for row in rows]


def rebuild_strategy_financials(
    strategy_instance_id: str,
) -> FinancialProjectionRebuildResponse:
    load_strategy(strategy_instance_id)
    with connection() as db:
        fact_count = db.execute(
            "SELECT COUNT(*) AS count FROM financial_facts WHERE strategy_instance_id = ?",
            (strategy_instance_id,),
        ).fetchone()["count"]
        pairs = db.execute(
            """
            SELECT DISTINCT account_id, instrument_id
            FROM financial_facts
            WHERE strategy_instance_id = ?
              AND fact_type IN ('trade_fill', 'deal', 'funding', 'swap', 'fee', 'fx')
              AND account_id IS NOT NULL
              AND instrument_id IS NOT NULL
            """,
            (strategy_instance_id,),
        ).fetchall()
        db.execute("DELETE FROM formal_positions WHERE strategy_instance_id = ?", (strategy_instance_id,))
        db.execute("DELETE FROM formal_pnl_results WHERE strategy_instance_id = ?", (strategy_instance_id,))

    for pair in pairs:
        rebuild_account_instrument_projection(
            strategy_instance_id,
            pair["account_id"],
            pair["instrument_id"],
        )

    completed_at = now_iso()
    with connection() as db:
        db.execute(
            """
            INSERT INTO audit_events (
                id, event_type, subject_type, subject_id, details_json, created_at
            ) VALUES (?, ?, ?, ?, ?, ?)
            """,
            (
                str(uuid4()),
                "formal_financial_projection_rebuilt",
                "strategy_instance",
                strategy_instance_id,
                json.dumps(
                    {"rebuiltPairCount": len(pairs), "factCount": fact_count},
                    sort_keys=True,
                ),
                completed_at,
            ),
        )
    return FinancialProjectionRebuildResponse(
        strategyInstanceId=strategy_instance_id,
        rebuiltPairCount=len(pairs),
        factCount=fact_count,
        completedAt=completed_at,
    )


def rebuild_account_instrument_projection(
    strategy_instance_id: str,
    account_id: str,
    instrument_id: str,
) -> None:
    if not account_id or not instrument_id:
        return
    with connection() as db:
        facts = db.execute(
            """
            SELECT * FROM financial_facts
            WHERE strategy_instance_id = ?
              AND account_id = ?
              AND instrument_id = ?
              AND fact_type IN ('trade_fill', 'deal', 'funding', 'swap', 'fee', 'fx')
            ORDER BY occurred_at, created_at, id
            """,
            (strategy_instance_id, account_id, instrument_id),
        ).fetchall()
    if not facts:
        return

    base_currency = facts[0]["base_currency"]
    quantity_unit = next(
        (row["quantity_unit"] for row in facts if row["quantity_unit"] is not None),
        "unknown",
    )
    old_quantity = Decimal("0")
    old_average: Decimal | None = None
    trading_pnl = Decimal("0")
    funding_pnl = Decimal("0")
    swap_pnl = Decimal("0")
    fee_pnl = Decimal("0")
    fx_pnl = Decimal("0")
    incomplete = False
    has_trade = False

    for row in facts:
        fact_type = row["fact_type"]
        if fact_type in TRADE_FACT_TYPES:
            has_trade = True
            signed_fill = Decimal(row["quantity"])
            if row["side"] == "sell":
                signed_fill = -signed_fill
            old_quantity, old_average, realized_native = calculate_position_update(
                old_quantity=old_quantity,
                old_average=old_average,
                signed_fill=signed_fill,
                fill_price=Decimal(row["price"]),
            )
            realized_native *= Decimal(row["contract_multiplier"])
            conversion = conversion_rate(row)
            if conversion is None:
                incomplete = True
            else:
                trading_pnl += realized_native * conversion
        else:
            converted = optional_decimal(row["converted_amount"])
            if converted is None:
                incomplete = True
                continue
            if fact_type == "funding":
                funding_pnl += converted
            elif fact_type == "swap":
                swap_pnl += converted
            elif fact_type == "fee":
                fee_pnl += converted
            elif fact_type == "fx":
                fx_pnl += converted

    quality = "incomplete" if incomplete else "complete"
    total_pnl = trading_pnl + funding_pnl + swap_pnl + fee_pnl + fx_pnl
    updated_at = facts[-1]["occurred_at"]
    with connection() as db:
        if has_trade:
            db.execute(
                """
                INSERT INTO formal_positions (
                    strategy_instance_id, account_id, instrument_id, net_quantity,
                    average_price, quantity_unit, data_quality_state, updated_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?)
                ON CONFLICT(strategy_instance_id, account_id, instrument_id) DO UPDATE SET
                    net_quantity = excluded.net_quantity,
                    average_price = excluded.average_price,
                    quantity_unit = excluded.quantity_unit,
                    data_quality_state = excluded.data_quality_state,
                    updated_at = excluded.updated_at
                """,
                (
                    strategy_instance_id,
                    account_id,
                    instrument_id,
                    decimal_text(old_quantity),
                    decimal_text(old_average) if old_average is not None else None,
                    quantity_unit,
                    quality,
                    updated_at,
                ),
            )
        db.execute(
            """
            INSERT INTO formal_pnl_results (
                strategy_instance_id, account_id, instrument_id, currency,
                trading_pnl, funding_pnl, swap_pnl, fee_pnl, fx_pnl, total_pnl,
                fact_count, data_quality_state, updated_at
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
            ON CONFLICT(strategy_instance_id, account_id, instrument_id) DO UPDATE SET
                currency = excluded.currency,
                trading_pnl = excluded.trading_pnl,
                funding_pnl = excluded.funding_pnl,
                swap_pnl = excluded.swap_pnl,
                fee_pnl = excluded.fee_pnl,
                fx_pnl = excluded.fx_pnl,
                total_pnl = excluded.total_pnl,
                fact_count = excluded.fact_count,
                data_quality_state = excluded.data_quality_state,
                updated_at = excluded.updated_at
            """,
            (
                strategy_instance_id,
                account_id,
                instrument_id,
                base_currency,
                decimal_text(trading_pnl),
                decimal_text(funding_pnl),
                decimal_text(swap_pnl),
                decimal_text(fee_pnl),
                decimal_text(fx_pnl),
                decimal_text(total_pnl),
                len(facts),
                quality,
                updated_at,
            ),
        )


def conversion_rate(row) -> Decimal | None:
    if row["currency"] == row["base_currency"]:
        return Decimal("1")
    return optional_decimal(row["fx_rate_to_base"])


def calculate_position_update(
    *,
    old_quantity: Decimal,
    old_average: Decimal | None,
    signed_fill: Decimal,
    fill_price: Decimal,
) -> tuple[Decimal, Decimal | None, Decimal]:
    if old_quantity == 0 or old_quantity * signed_fill > 0:
        new_quantity = old_quantity + signed_fill
        old_notional = abs(old_quantity) * (old_average or Decimal("0"))
        new_notional = abs(signed_fill) * fill_price
        new_average = (old_notional + new_notional) / abs(new_quantity)
        return new_quantity, new_average, Decimal("0")

    closing_quantity = min(abs(old_quantity), abs(signed_fill))
    direction = Decimal("1") if old_quantity > 0 else Decimal("-1")
    realized_pnl = closing_quantity * (fill_price - (old_average or fill_price)) * direction
    new_quantity = old_quantity + signed_fill
    if new_quantity == 0:
        return new_quantity, None, realized_pnl
    if old_quantity * new_quantity > 0:
        return new_quantity, old_average, realized_pnl
    return new_quantity, fill_price, realized_pnl


def list_formal_pnl(strategy_instance_id: str) -> list[FormalPnlResponse]:
    load_strategy(strategy_instance_id)
    with connection() as db:
        rows = db.execute(
            """
            SELECT * FROM formal_pnl_results
            WHERE strategy_instance_id = ?
            ORDER BY account_id, instrument_id
            """,
            (strategy_instance_id,),
        ).fetchall()
    return [formal_pnl_from_row(row) for row in rows]


def list_formal_positions(strategy_instance_id: str) -> list[FormalPositionResponse]:
    load_strategy(strategy_instance_id)
    with connection() as db:
        rows = db.execute(
            """
            SELECT * FROM formal_positions
            WHERE strategy_instance_id = ?
            ORDER BY account_id, instrument_id
            """,
            (strategy_instance_id,),
        ).fetchall()
    return [formal_position_from_row(row) for row in rows]


def run_formal_nav_snapshot(
    strategy_instance_id: str,
    valuation_time: datetime | None = None,
) -> FormalNavSnapshotResponse:
    strategy = load_strategy(strategy_instance_id)
    capital_base = optional_decimal(strategy["capital_base"])
    if capital_base is None or capital_base <= 0:
        raise HTTPException(status_code=422, detail="Strategy instance has no valid capital base")
    valuation_iso = utc_iso(valuation_time)

    with connection() as db:
        accounts = db.execute(
            """
            SELECT DISTINCT sab.account_id
            FROM strategy_account_bindings sab
            JOIN accounts a ON a.id = sab.account_id
            WHERE sab.strategy_instance_id = ?
              AND sab.status = 'active'
              AND a.status = 'active'
            ORDER BY sab.account_id
            """,
            (strategy_instance_id,),
        ).fetchall()
    if not accounts:
        raise HTTPException(status_code=422, detail="Strategy has no active account bindings")

    equity = Decimal("0")
    included = 0
    missing: list[str] = []
    with connection() as db:
        for account in accounts:
            row = db.execute(
                """
                SELECT converted_amount
                FROM financial_facts
                WHERE strategy_instance_id = ?
                  AND account_id = ?
                  AND fact_type = 'balance'
                  AND occurred_at <= ?
                ORDER BY occurred_at DESC, created_at DESC
                LIMIT 1
                """,
                (strategy_instance_id, account["account_id"], valuation_iso),
            ).fetchone()
            if row is None or row["converted_amount"] is None:
                missing.append(account["account_id"])
                continue
            equity += Decimal(row["converted_amount"])
            included += 1

    required = len(accounts)
    quality = "complete" if included == required else ("partial" if included else "incomplete")
    equity_value = equity if included else None
    nav = equity / capital_base if included else None
    snapshot_id = str(uuid4())
    created_at = now_iso()
    with connection() as db:
        db.execute(
            """
            INSERT INTO formal_strategy_nav_snapshots (
                id, strategy_instance_id, valuation_time, equity, capital_base, nav,
                currency, data_quality_state, required_account_count,
                included_account_count, missing_account_ids_json, created_at
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                snapshot_id,
                strategy_instance_id,
                valuation_iso,
                decimal_text(equity_value) if equity_value is not None else None,
                decimal_text(capital_base),
                decimal_text(nav) if nav is not None else None,
                strategy["base_currency"],
                quality,
                required,
                included,
                json.dumps(missing, sort_keys=True),
                created_at,
            ),
        )
        db.execute(
            """
            INSERT INTO audit_events (
                id, event_type, subject_type, subject_id, details_json, created_at
            ) VALUES (?, ?, ?, ?, ?, ?)
            """,
            (
                str(uuid4()),
                "formal_nav_snapshot_created",
                "strategy_instance",
                strategy_instance_id,
                json.dumps(
                    {
                        "valuationTime": valuation_iso,
                        "requiredAccountCount": required,
                        "includedAccountCount": included,
                        "missingAccountIds": missing,
                        "dataQualityState": quality,
                    },
                    sort_keys=True,
                ),
                created_at,
            ),
        )
        row = db.execute(
            "SELECT * FROM formal_strategy_nav_snapshots WHERE id = ?",
            (snapshot_id,),
        ).fetchone()
    return formal_nav_from_row(row)


def list_formal_nav_snapshots(strategy_instance_id: str) -> list[FormalNavSnapshotResponse]:
    load_strategy(strategy_instance_id)
    with connection() as db:
        rows = db.execute(
            """
            SELECT * FROM formal_strategy_nav_snapshots
            WHERE strategy_instance_id = ?
            ORDER BY valuation_time DESC
            """,
            (strategy_instance_id,),
        ).fetchall()
    return [formal_nav_from_row(row) for row in rows]


def financial_fact_from_row(row) -> FinancialFactResponse:
    return FinancialFactResponse(
        factId=row["id"],
        idempotencyKey=row["idempotency_key"],
        factType=row["fact_type"],
        source=row["source"],
        externalId=row["external_id"],
        strategyInstanceId=row["strategy_instance_id"],
        accountId=row["account_id"],
        instrumentId=row["instrument_id"],
        side=row["side"],
        quantity=optional_decimal(row["quantity"]),
        quantityUnit=row["quantity_unit"],
        price=optional_decimal(row["price"]),
        contractMultiplier=optional_decimal(row["contract_multiplier"]),
        amount=optional_decimal(row["amount"]),
        currency=row["currency"],
        baseCurrency=row["base_currency"],
        fxRateToBase=optional_decimal(row["fx_rate_to_base"]),
        convertedAmount=optional_decimal(row["converted_amount"]),
        availableBalance=optional_decimal(row["available_balance"]),
        occurredAt=row["occurred_at"],
        dataQualityState=row["data_quality_state"],
        createdAt=row["created_at"],
    )


def formal_pnl_from_row(row) -> FormalPnlResponse:
    return FormalPnlResponse(
        strategyInstanceId=row["strategy_instance_id"],
        accountId=row["account_id"],
        instrumentId=row["instrument_id"],
        currency=row["currency"],
        tradingPnl=Decimal(row["trading_pnl"]),
        fundingPnl=Decimal(row["funding_pnl"]),
        swapPnl=Decimal(row["swap_pnl"]),
        feePnl=Decimal(row["fee_pnl"]),
        fxPnl=Decimal(row["fx_pnl"]),
        totalPnl=Decimal(row["total_pnl"]),
        factCount=row["fact_count"],
        dataQualityState=row["data_quality_state"],
        updatedAt=row["updated_at"],
    )


def formal_position_from_row(row) -> FormalPositionResponse:
    return FormalPositionResponse(
        strategyInstanceId=row["strategy_instance_id"],
        accountId=row["account_id"],
        instrumentId=row["instrument_id"],
        netQuantity=Decimal(row["net_quantity"]),
        averagePrice=optional_decimal(row["average_price"]),
        quantityUnit=row["quantity_unit"],
        dataQualityState=row["data_quality_state"],
        updatedAt=row["updated_at"],
    )


def formal_nav_from_row(row) -> FormalNavSnapshotResponse:
    return FormalNavSnapshotResponse(
        snapshotId=row["id"],
        strategyInstanceId=row["strategy_instance_id"],
        valuationTime=row["valuation_time"],
        equity=optional_decimal(row["equity"]),
        capitalBase=Decimal(row["capital_base"]),
        nav=optional_decimal(row["nav"]),
        currency=row["currency"],
        dataQualityState=row["data_quality_state"],
        requiredAccountCount=row["required_account_count"],
        includedAccountCount=row["included_account_count"],
        missingAccountIds=json.loads(row["missing_account_ids_json"]),
        createdAt=row["created_at"],
    )
'''
          Path('platform-backend/app/financial_facts.py').write_text(
              financial_facts_module,
              encoding='utf-8',
          )

          replace_once(
              'platform-backend/app/main.py',
              'from contextlib import asynccontextmanager\nfrom decimal import Decimal\n',
              'from contextlib import asynccontextmanager\nfrom datetime import datetime\nfrom decimal import Decimal\n',
          )
          replace_once(
              'platform-backend/app/main.py',
              'from app.execution_batches import (\n    create_execution_batch,\n    get_execution_batch,\n    list_execution_batches,\n)\n',
              'from app.execution_batches import (\n    create_execution_batch,\n    get_execution_batch,\n    list_execution_batches,\n)\nfrom app.financial_facts import (\n    list_financial_facts,\n    list_formal_nav_snapshots,\n    list_formal_pnl,\n    list_formal_positions,\n    rebuild_strategy_financials,\n    record_financial_fact,\n    run_formal_nav_snapshot,\n)\n',
          )
          replace_once(
              'platform-backend/app/main.py',
              '    FillResponse,\n    InstrumentResponse,\n',
              '    FillResponse,\n    FinancialFactResponse,\n    FinancialProjectionRebuildResponse,\n    FormalNavSnapshotResponse,\n    FormalPnlResponse,\n    FormalPositionResponse,\n    InstrumentResponse,\n',
          )
          replace_once(
              'platform-backend/app/main.py',
              '    CreateExecutionBatchRequest,\n    CreateOrderRequest,\n',
              '    CreateExecutionBatchRequest,\n    CreateFinancialFactRequest,\n    CreateOrderRequest,\n',
          )

          phase3_routes = r'''
          @app.post(
              f"{settings.api_prefix}/financial-facts",
              response_model=FinancialFactResponse,
              tags=["financial-facts"],
              summary="Record one immutable external financial fact",
          )
          def create_financial_fact(
              request: CreateFinancialFactRequest,
          ) -> FinancialFactResponse:
              return record_financial_fact(request)


          @app.get(
              f"{settings.api_prefix}/financial-facts",
              response_model=list[FinancialFactResponse],
              tags=["financial-facts"],
          )
          def financial_facts(
              strategy_instance_id: str | None = Query(default=None, alias="strategyInstanceId"),
              fact_type: str | None = Query(default=None, alias="factType"),
              limit: int = Query(default=200, ge=1, le=1000),
          ) -> list[FinancialFactResponse]:
              return list_financial_facts(strategy_instance_id, fact_type, limit)


          @app.post(
              f"{settings.api_prefix}/strategies/instances/{{strategy_instance_id}}/financials/rebuild",
              response_model=FinancialProjectionRebuildResponse,
              tags=["financial-facts"],
          )
          def rebuild_financials(
              strategy_instance_id: str,
          ) -> FinancialProjectionRebuildResponse:
              return rebuild_strategy_financials(strategy_instance_id)


          @app.get(
              f"{settings.api_prefix}/strategies/instances/{{strategy_instance_id}}/formal-pnl",
              response_model=list[FormalPnlResponse],
              tags=["pnl"],
          )
          def formal_pnl(strategy_instance_id: str) -> list[FormalPnlResponse]:
              return list_formal_pnl(strategy_instance_id)


          @app.get(
              f"{settings.api_prefix}/strategies/instances/{{strategy_instance_id}}/formal-positions",
              response_model=list[FormalPositionResponse],
              tags=["pnl"],
          )
          def formal_positions(strategy_instance_id: str) -> list[FormalPositionResponse]:
              return list_formal_positions(strategy_instance_id)


          @app.get(
              f"{settings.api_prefix}/strategies/instances/{{strategy_instance_id}}/formal-nav-snapshots",
              response_model=list[FormalNavSnapshotResponse],
              tags=["pnl"],
          )
          def formal_nav_snapshots(
              strategy_instance_id: str,
          ) -> list[FormalNavSnapshotResponse]:
              return list_formal_nav_snapshots(strategy_instance_id)


          @app.post(
              f"{settings.api_prefix}/strategies/instances/{{strategy_instance_id}}/formal-nav-snapshots/run",
              response_model=FormalNavSnapshotResponse,
              tags=["pnl"],
          )
          def run_formal_nav(
              strategy_instance_id: str,
              valuation_time: datetime | None = Query(default=None, alias="valuationTime"),
          ) -> FormalNavSnapshotResponse:
              return run_formal_nav_snapshot(strategy_instance_id, valuation_time)


          '''
          replace_once(
              'platform-backend/app/main.py',
              '@app.get(\n    f"{settings.api_prefix}/accounts/{{account_id}}/positions/{{instrument_id}}",',
              phase3_routes + '@app.get(\n    f"{settings.api_prefix}/accounts/{{account_id}}/positions/{{instrument_id}}",',
          )

          tests = r'''from pathlib import Path

from fastapi.testclient import TestClient

from app.config import get_settings
from app.database import connection
from app.main import app

STRATEGY_ID = "strategy_funding_arbitrage_instance_default"
ACCOUNT_ID = "account_sim_usdt"
INSTRUMENT_ID = "instrument_btc_usdt"


def fact_payload(
    *,
    key: str,
    fact_type: str,
    external_id: str,
    occurred_at: str,
    **extra,
) -> dict[str, object]:
    return {
        "idempotencyKey": key,
        "factType": fact_type,
        "source": "golden-test",
        "externalId": external_id,
        "strategyInstanceId": STRATEGY_ID,
        "accountId": ACCOUNT_ID,
        "instrumentId": INSTRUMENT_ID,
        "occurredAt": occurred_at,
        **extra,
    }


def test_financial_facts_are_idempotent_rebuildable_and_componentized(
    tmp_path: Path,
) -> None:
    get_settings().database_path = str(tmp_path / "financial-facts.db")
    with TestClient(app) as client:
        with connection() as db:
            db.execute(
                "UPDATE contract_specifications SET contract_multiplier = '10' WHERE instrument_id = ?",
                (INSTRUMENT_ID,),
            )

        buy = fact_payload(
            key="fill-buy-1",
            fact_type="trade_fill",
            external_id="fill-buy-1",
            occurred_at="2026-07-23T01:00:00+00:00",
            side="buy",
            quantity="2",
            price="100",
        )
        sell = fact_payload(
            key="fill-sell-1",
            fact_type="trade_fill",
            external_id="fill-sell-1",
            occurred_at="2026-07-23T02:00:00+00:00",
            side="sell",
            quantity="1",
            price="110",
        )
        assert client.post("/api/v1/financial-facts", json=buy).status_code == 200
        assert client.post("/api/v1/financial-facts", json=sell).status_code == 200

        duplicate = client.post("/api/v1/financial-facts", json=sell)
        assert duplicate.status_code == 200
        conflict_payload = {**sell, "price": "111"}
        conflict = client.post("/api/v1/financial-facts", json=conflict_payload)
        assert conflict.status_code == 409

        for key, fact_type, amount, hour in [
            ("funding-1", "funding", "5", 3),
            ("swap-1", "swap", "2", 4),
            ("fee-1", "fee", "-1", 5),
            ("fx-1", "fx", "3", 6),
        ]:
            response = client.post(
                "/api/v1/financial-facts",
                json=fact_payload(
                    key=key,
                    fact_type=fact_type,
                    external_id=key,
                    occurred_at=f"2026-07-23T0{hour}:00:00+00:00",
                    amount=amount,
                    currency="USDT",
                ),
            )
            assert response.status_code == 200

        pnl_response = client.get(f"/api/v1/strategies/instances/{STRATEGY_ID}/formal-pnl")
        assert pnl_response.status_code == 200
        pnl = pnl_response.json()[0]
        assert pnl["tradingPnl"] == "100"
        assert pnl["fundingPnl"] == "5"
        assert pnl["swapPnl"] == "2"
        assert pnl["feePnl"] == "-1"
        assert pnl["fxPnl"] == "3"
        assert pnl["totalPnl"] == "109"
        assert pnl["factCount"] == 6
        assert pnl["dataQualityState"] == "complete"

        position_response = client.get(
            f"/api/v1/strategies/instances/{STRATEGY_ID}/formal-positions"
        )
        position = position_response.json()[0]
        assert position["netQuantity"] == "1"
        assert position["averagePrice"] == "100"
        assert position["quantityUnit"] == "BTC"

        with connection() as db:
            db.execute("DELETE FROM formal_positions WHERE strategy_instance_id = ?", (STRATEGY_ID,))
            db.execute("DELETE FROM formal_pnl_results WHERE strategy_instance_id = ?", (STRATEGY_ID,))
        rebuild = client.post(
            f"/api/v1/strategies/instances/{STRATEGY_ID}/financials/rebuild"
        )
        assert rebuild.status_code == 200
        assert rebuild.json()["rebuiltPairCount"] == 1
        rebuilt_pnl = client.get(
            f"/api/v1/strategies/instances/{STRATEGY_ID}/formal-pnl"
        ).json()[0]
        assert rebuilt_pnl["totalPnl"] == "109"

        facts = client.get(
            "/api/v1/financial-facts",
            params={"strategyInstanceId": STRATEGY_ID},
        )
        assert facts.status_code == 200
        assert len(facts.json()) == 6


def test_formal_nav_uses_one_valuation_time_and_reports_missing_accounts(
    tmp_path: Path,
) -> None:
    get_settings().database_path = str(tmp_path / "formal-nav.db")
    with TestClient(app) as client:
        second_account = "account_sim_usdt_secondary"
        with connection() as db:
            db.execute(
                """
                INSERT INTO accounts (
                    id, venue_id, account_code, name, account_type, environment,
                    base_currency, credential_ref, status, data_quality_state, created_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    second_account,
                    "venue_bybit",
                    "SIM-USDT-SECONDARY",
                    "Secondary Simulation",
                    "simulation",
                    "simulation",
                    "USDT",
                    None,
                    "active",
                    "complete",
                    "2026-07-23T00:00:00+00:00",
                ),
            )
            db.execute(
                """
                INSERT INTO strategy_account_bindings (
                    id, strategy_instance_id, account_id, role, max_notional, status, created_at
                ) VALUES (?, ?, ?, ?, ?, ?, ?)
                """,
                (
                    "binding_funding_secondary",
                    STRATEGY_ID,
                    second_account,
                    "secondary",
                    None,
                    "active",
                    "2026-07-23T00:00:00+00:00",
                ),
            )

        first_balance = {
            "idempotencyKey": "balance-primary-1",
            "factType": "balance",
            "source": "golden-test",
            "externalId": "balance-primary-1",
            "strategyInstanceId": STRATEGY_ID,
            "accountId": ACCOUNT_ID,
            "amount": "100000",
            "availableBalance": "90000",
            "currency": "USDT",
            "occurredAt": "2026-07-23T08:00:00+00:00",
        }
        assert client.post("/api/v1/financial-facts", json=first_balance).status_code == 200

        partial = client.post(
            f"/api/v1/strategies/instances/{STRATEGY_ID}/formal-nav-snapshots/run",
            params={"valuationTime": "2026-07-23T09:00:00+00:00"},
        )
        assert partial.status_code == 200
        assert partial.json()["dataQualityState"] == "partial"
        assert partial.json()["requiredAccountCount"] == 2
        assert partial.json()["includedAccountCount"] == 1
        assert partial.json()["missingAccountIds"] == [second_account]
        assert partial.json()["equity"] == "100000"
        assert partial.json()["nav"] == "1"

        second_balance = {
            "idempotencyKey": "balance-secondary-1",
            "factType": "balance",
            "source": "golden-test",
            "externalId": "balance-secondary-1",
            "strategyInstanceId": STRATEGY_ID,
            "accountId": second_account,
            "amount": "50000",
            "availableBalance": "45000",
            "currency": "USDT",
            "occurredAt": "2026-07-23T08:30:00+00:00",
        }
        assert client.post("/api/v1/financial-facts", json=second_balance).status_code == 200

        complete = client.post(
            f"/api/v1/strategies/instances/{STRATEGY_ID}/formal-nav-snapshots/run",
            params={"valuationTime": "2026-07-23T09:00:00+00:00"},
        )
        assert complete.status_code == 200
        assert complete.json()["dataQualityState"] == "complete"
        assert complete.json()["includedAccountCount"] == 2
        assert complete.json()["missingAccountIds"] == []
        assert complete.json()["equity"] == "150000"
        assert complete.json()["nav"] == "1.5"
'''
          Path('platform-backend/tests/test_financial_facts.py').write_text(tests, encoding='utf-8')

          replace_once(
              '.github/workflows/platform-ci.yml',
              '            app/execution_batches.py \\\n',
              '            app/execution_batches.py \\\n            app/financial_facts.py \\\n',
          )
          replace_once(
              '.github/workflows/platform-ci.yml',
              '            tests/test_execution_batches_v1.py \\\n',
              '            tests/test_execution_batches_v1.py \\\n            tests/test_financial_facts.py \\\n',
          )

          phase3_plan = r'''# V6 Phase 3：金融事实与正式账务

状态：`implementation complete / acceptance pending`  
实施分支：`hardening/v6-financial-facts-phase3`  
跟踪 Issue：`#7 V6 Phase 3：不可变金融事实、正式 PnL 与统一估值 NAV`  
总计划：`V6-交易安全加固实施计划.md`  
更新时间：`2026-07-23`

## 1. 本阶段目标

Phase 3 不继续扩张策略和页面，而是建立最小但可审计的正式账务链路：

```text
不可变 FinancialFact
→ ContractSpecification / Currency / Unit / FX 快照
→ 可重复重建的 Position 与 PnL
→ 同一 valuationTime 的多账户 NAV
```

当前仍只允许 Simulation / Fake Gateway，不开放 Paper、Demo 或 Live。

## 2. 不可变事实模型

统一事实表 `financial_facts` 支持：

- `external_order`
- `trade_fill`
- `deal`
- `funding`
- `swap`
- `fee`
- `balance`
- `position`
- `fx`

每条事实必须具有：

- 客户端 `idempotencyKey`
- `source + externalId + factType` 外部身份
- StrategyInstance、Account、Instrument 归属
- 事件时间与入库时间
- 币种、数量、单位、合约乘数和汇率快照
- 内容哈希与数据质量状态

事实只允许新增，不提供更新和删除 API。同一身份只有载荷完全一致时才返回原记录；载荷冲突返回 409。

## 3. 正式投影

### 3.1 Position

`formal_positions` 由 Trade Fill / Deal 事实按事件时间重放生成。数量使用 Instrument 的 `quantityUnit`，平均价使用事实价格，重复导入不改变投影。

### 3.2 PnL

`formal_pnl_results` 分项保存：

- Trading PnL
- Funding PnL
- Swap PnL
- Fee PnL
- FX PnL
- Total PnL

Trade PnL 使用成交数量、成交价和 ContractSpecification 的 `contractMultiplier`。跨币种事实必须携带 `fxRateToBase`；缺失汇率时保留事实，但投影标记 `incomplete`，不得把缺失值当作零或自动把 Stablecoin 当作 USD。

### 3.3 NAV

`formal_strategy_nav_snapshots` 在调用方指定的同一 `valuationTime` 下，为每个 active binding 账户选择不晚于该时点的最新 Balance Fact。

响应明确返回：

- requiredAccountCount
- includedAccountCount
- missingAccountIds
- complete / partial / incomplete

没有任何可用余额时，equity 和 nav 返回空值，不伪装为零。

## 4. API

```http
POST /api/v1/financial-facts
GET  /api/v1/financial-facts
POST /api/v1/strategies/instances/{strategyInstanceId}/financials/rebuild
GET  /api/v1/strategies/instances/{strategyInstanceId}/formal-positions
GET  /api/v1/strategies/instances/{strategyInstanceId}/formal-pnl
GET  /api/v1/strategies/instances/{strategyInstanceId}/formal-nav-snapshots
POST /api/v1/strategies/instances/{strategyInstanceId}/formal-nav-snapshots/run
```

旧 `/pnl` 与 `/nav-snapshots` 端点暂时保留为工程兼容口径，但不得称为正式投资账务。新开发和核对必须使用 `formal-*` 端点。

## 5. 重建与审计

`financials/rebuild` 会清空指定 StrategyInstance 的正式 Position / PnL 投影，再从不可变事实完整重放。事实本身不会被修改。

以下操作写入 AuditEvent：

- FinancialFact 入库
- 正式投影重建
- 正式 NAV 快照创建

## 6. 验收

- [x] 事实重复导入不重复计入。
- [x] 相同身份不同载荷返回 409。
- [x] Position 和 PnL 可清空后重建。
- [x] Contract Multiplier 进入 Trading PnL。
- [x] Trading / Funding / Swap / Fee / FX 分项可查询。
- [x] NAV 按同一 valuationTime 汇总全部 active binding 账户。
- [x] 缺失账户和汇率显式标记，不自动当零。
- [x] 资费套利金样本覆盖事实、重放、PnL 和 NAV。
- [ ] 最终 Platform CI 通过并记录 Run ID。
- [ ] PR、Issue、Changelog 和总计划完成最终验收留痕。

## 7. 明确延期

Phase 3 不处理：

- Bybit / MT5 主动拉取和自动导入事实。
- 外部账单文件解析器。
- 双腿残留敞口自动平仓和 Kill Switch。
- 用户认证、RBAC、双人审批和生产密钥托管。
- Kafka、Kubernetes 或完整 Event Sourcing 框架。

这些内容进入 Phase 4 或后续运维阶段。
'''
          Path('docs/planning/V6-Phase3-金融事实与正式账务.md').write_text(
              phase3_plan,
              encoding='utf-8',
          )

          technical_doc = r'''# Financial Facts and Formal Accounting

状态：`active`  
适用版本：`Platform V6 / Phase 3`

## 权威边界

- `financial_facts` 是外部金融事实的不可变入口。
- `formal_positions`、`formal_pnl_results`、`formal_strategy_nav_snapshots` 是可删除、可重建的投影。
- 事实身份由客户端幂等键和外部三元组 `source + externalId + factType` 双重约束。
- 投影不得反向修改事实。

## 金融数值

- 金额、价格、数量和汇率以十进制字符串通过 API 传输。
- Trade Fill 的 Contract Multiplier 和 Quantity Unit 由后端 Instrument Catalog 快照确定，不接受前端覆盖。
- Monetary Fact 的 amount 是带符号经济贡献；费用通常以负数传入。
- 非基础币种必须提供 `fxRateToBase`，否则事实保留但正式投影标记 incomplete。
- Stablecoin 与法币不自动等价。

## 重放顺序

同一 Strategy / Account / Instrument 按：

```text
occurred_at → created_at → fact id
```

重放。Position 与 Trading PnL 使用同一成交事实序列，避免两套顺序产生不同结果。

## NAV 时间语义

NAV 只使用 `occurred_at <= valuationTime` 的 Balance Fact；每个 active binding 账户取该时点之前最新一条。数据覆盖不足时返回 missingAccountIds 和 partial / incomplete，不进行静默补零。
'''
          Path('docs/technical/FINANCIAL_FACTS.md').write_text(technical_doc, encoding='utf-8')

          replace_once(
              'docs/planning/V6-交易安全加固实施计划.md',
              '| Phase 3 | 外部事实层、合约规格、PnL 与 NAV 重构 | 待实施 | PnL/NAV 可用于内部核对 |',
              '| Phase 3 | 不可变事实、合约规格、正式 PnL 与统一估值 NAV | 实现完成，等待最终验收 | PnL/NAV 可用于内部核对 |',
          )
          replace_once(
              'docs/planning/V6-交易安全加固实施计划.md',
              'Phase 3 只处理金融事实和账务正确性：',
              'Phase 3 实施记录见 `V6-Phase3-金融事实与正式账务.md`。本阶段只处理金融事实和账务正确性：',
          )

          append_once(
              'docs/technical/API_SPEC.md',
              '## 11. Phase 3 不可变金融事实',
              r'''## 11. Phase 3 不可变金融事实

```http
POST /api/v1/financial-facts
GET  /api/v1/financial-facts?strategyInstanceId=...&factType=...
```

事实写入规则：

- `idempotencyKey` 必填。
- `source + externalId + factType` 构成外部身份。
- 重复且载荷一致返回原事实；身份冲突返回 409。
- Trade Fill / Deal 的 Currency、Quantity Unit 和 Contract Multiplier 由 Instrument Catalog 确定。
- 跨币种金额必须提供 `fxRateToBase`，否则事实保留但数据质量为 incomplete。
- 不提供事实更新或删除 API。

## 12. Phase 3 正式 Position、PnL 与 NAV

```http
POST /api/v1/strategies/instances/{strategyInstanceId}/financials/rebuild
GET  /api/v1/strategies/instances/{strategyInstanceId}/formal-positions
GET  /api/v1/strategies/instances/{strategyInstanceId}/formal-pnl
GET  /api/v1/strategies/instances/{strategyInstanceId}/formal-nav-snapshots
POST /api/v1/strategies/instances/{strategyInstanceId}/formal-nav-snapshots/run?valuationTime=...
```

- Formal Position / PnL 可以从 FinancialFact 完整重建。
- PnL 分为 Trading、Funding、Swap、Fee、FX 和 Total。
- Formal NAV 对全部 active binding 账户使用同一 valuationTime。
- 响应返回 requiredAccountCount、includedAccountCount 和 missingAccountIds。
- 旧 `/pnl` 与 `/nav-snapshots` 继续作为工程兼容接口，不能作为正式账务口径。
''',
          )

          replace_once(
              'README.md',
              '- 当前实施：Phase 2 命令入口与结果恢复\n',
              '- 当前实施：Phase 3 不可变金融事实、正式 PnL 与统一估值 NAV\n',
          )
          replace_once(
              'README.md',
              '- Phase 2：Issue `#4`、PR `#5`\n',
              '- Phase 2：Issue `#4`、PR `#5`，已完成\n- Phase 3：Issue `#7`，实施分支 `hardening/v6-financial-facts-phase3`\n',
          )
          replace_once(
              'README.md',
              '| 看当前 Phase 2 | `docs/planning/V6-Phase2-命令入口与结果恢复.md` |\n',
              '| 看 Phase 2 | `docs/planning/V6-Phase2-命令入口与结果恢复.md` |\n| 看当前 Phase 3 | `docs/planning/V6-Phase3-金融事实与正式账务.md` |\n| 看正式金融事实设计 | `docs/technical/FINANCIAL_FACTS.md` |\n',
          )

          replace_once(
              'admin-risk/docs/START-HERE.md',
              '3. `modules/一级模块定位总表.md`',
              '3. `../../docs/planning/V6-Phase3-金融事实与正式账务.md`\n4. `modules/一级模块定位总表.md`',
          )
          replace_once(
              'admin-risk/docs/START-HERE.md',
              '4. `modules/策略-需求文档.md`\n5. `modules/交易平台-需求文档.md`\n6. `modules/策略管理-需求文档.md`\n7. `architecture/platform-target-architecture.md`\n8. `architecture/domain/domain-overview.md`\n9. `architecture/reference-code-adoption-matrix.md`\n10. `planning/V1-开发路线图.md`',
              '5. `modules/策略-需求文档.md`\n6. `modules/交易平台-需求文档.md`\n7. `modules/策略管理-需求文档.md`\n8. `architecture/platform-target-architecture.md`\n9. `architecture/domain/domain-overview.md`\n10. `architecture/reference-code-adoption-matrix.md`\n11. `planning/V1-开发路线图.md`',
          )
          replace_once(
              'admin-risk/docs/START-HERE.md',
              '| P2 | 外部事实层、合约规格、PnL/NAV 重构 | 待实施 |',
              '| P2 | 不可变事实、合约规格、正式 PnL/NAV | 实现完成，等待最终验收 |',
          )

          replace_once(
              'admin-risk/docs/quality/release-gate.md',
              '当前阶段：`../../../docs/planning/V6-Phase2-命令入口与结果恢复.md`',
              '当前阶段：`../../../docs/planning/V6-Phase3-金融事实与正式账务.md`',
          )
          replace_once(
              'admin-risk/docs/quality/release-gate.md',
              '当前 V6 的 PnL/NAV 尚未完成上述全部口径，因此只能用于工程演示，不能作为正式投资账务。',
              'Phase 3 新增 `formal-*` 口径用于内部核对；旧 PnL/NAV 接口仍是工程兼容口径。任何数据质量为 partial / incomplete 的结果不得解释为完整投资账务。',
          )
          append_once(
              'admin-risk/docs/quality/release-gate.md',
              '## 14. Phase 3 金融事实与正式账务检查',
              r'''## 14. Phase 3 金融事实与正式账务检查

- FinancialFact 只能新增，不能通过 API 更新或删除。
- idempotencyKey 与外部身份重复时必须验证载荷一致性。
- Position / PnL 清空后可以从事实完整重建。
- Trading PnL 必须使用 Contract Multiplier。
- Funding、Swap、Fee、FX 与 Trading PnL 分项保存。
- 非基础币种缺失 FX 时必须标记 incomplete。
- Stablecoin 不自动等同法币。
- Formal NAV 对全部 active binding 账户使用同一 valuationTime。
- 账户数据缺失必须返回 missingAccountIds，不能静默补零。
- 旧 PnL/NAV 兼容接口不得在产品和文档中标记为正式账务。
''',
          )

          replace_once(
              'CHANGELOG.md',
              '## Unreleased\n',
              '''## Unreleased\n\n### Immutable financial facts and formal accounting — Phase 3\n\n- Added immutable `financial_facts` with dual idempotency, content hashes, external identities, event time, Catalog-derived units and contract multipliers, currency conversion snapshots, and explicit data quality.\n- Added rebuildable `formal_positions` and componentized `formal_pnl_results` for Trading, Funding, Swap, Fee, FX, and Total PnL.\n- Added `formal_strategy_nav_snapshots` using one valuation time across all active strategy-account bindings with explicit account coverage and missing-account disclosure.\n- Added fact ingestion, list, projection rebuild, formal Position/PnL, and formal NAV APIs.\n- Added golden tests for duplicate fact ingestion, payload conflicts, Contract Multiplier PnL, component attribution, projection rebuild, and multi-account NAV completeness.\n- Added `docs/planning/V6-Phase3-金融事实与正式账务.md` and `docs/technical/FINANCIAL_FACTS.md`; synchronized README, START-HERE, API Spec, Release Gate, overall plan, CI gate, Issue #7, and this Changelog.\n- Retained Simulation / Fake Gateway only; external venue ingestion, residual-leg risk handling, and Live remain deferred.\n''',
          )
          PY
      - name: Install backend dependencies
        working-directory: platform-backend
        run: python -m pip install -e '.[dev]'
      - name: Validate backend
        working-directory: platform-backend
        run: |
          python -m ruff check app tests
          python -m pytest
      - name: Commit generated implementation
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/phase3-codegen.yml
          git add .
          git commit -m "feat: add immutable financial facts and formal accounting"
          git push origin HEAD:hardening/v6-financial-facts-phase3
