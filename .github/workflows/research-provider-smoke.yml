name: Research Provider Smoke

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/research-provider-smoke.yml'
      - 'platform-backend/app/research_providers.py'
      - 'platform-backend/scripts/smoke_research_providers.py'
      - 'platform-backend/pyproject.toml'

permissions:
  contents: read

concurrency:
  group: research-provider-smoke-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  live-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: platform-backend/pyproject.toml

      - name: Install Platform Backend
        run: python -m pip install -e './platform-backend[dev]'

      - name: Probe free research providers
        working-directory: platform-backend
        shell: bash
        run: |
          set -o pipefail
          python scripts/smoke_research_providers.py --timeout 60 | tee research-provider-smoke.json

      - name: Publish result summary
        if: always()
        working-directory: platform-backend
        shell: bash
        run: |
          if [ -f research-provider-smoke.json ]; then
            echo '## Research Provider Smoke' >> "$GITHUB_STEP_SUMMARY"
            echo '```json' >> "$GITHUB_STEP_SUMMARY"
            cat research-provider-smoke.json >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload smoke result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: research-provider-smoke
          path: platform-backend/research-provider-smoke.json
          if-no-files-found: warn
          retention-days: 3
