# 早期 DOCX 归档说明

该目录用于保存项目早期形成、已不再作为当前开发依据的 DOCX 文档。

待本地确认并迁移的文件包括：

- `核心模块.docx`
- `需求文档.docx`

处理流程：

1. 在本地确认文件的精确路径和内容。
2. 检查是否存在尚未写入 active Markdown 文档的唯一业务规则。
3. 将仍有效内容迁移到对应模块或策略文档。
4. 将原 DOCX 移入本目录，可重命名为 `[已归档]原文件名.docx`。
5. 保留历史文件，不再让 AI 或 Codex 将其作为当前需求来源。
