HOST=127.0.0.1
PORT=8080
DB_DSN=risk_app:replace_with_db_password@tcp(127.0.0.1:3306)/risk_control?parseTime=true&loc=Asia%2FShanghai
JWT_SECRET=replace_with_at_least_32_random_characters

# Enable only for the first start if an administrator must be created.
SEED_ADMIN=true
SEED_USERNAME=admin
SEED_PASSWORD=replace_with_a_strong_admin_password
