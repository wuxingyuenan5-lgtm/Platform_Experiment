# Ubuntu 系统软件安装指令（私募风控平台开发环境）

以下是在 **Ubuntu 20.04/22.04 LTS** 上为私募交易风控平台准备开发和运行环境所需的完整安装指令。请按顺序执行。

---

## 1. 系统基础更新与工具

```bash
# 更新系统包索引并升级已安装的包
sudo apt update && sudo apt upgrade -y

# 安装常用工具
sudo apt install -y curl wget git vim htop net-tools build-essential
```

---

## 2. 安装 Go 语言环境（后端开发必需）

```bash
# 下载并安装 Go 1.21+（适用于所有四个微服务）
cd /tmp
wget https://go.dev/dl/go1.21.6.linux-amd64.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf go1.21.6.linux-amd64.tar.gz

# 配置环境变量
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
echo 'export GOPATH=$HOME/go' >> ~/.bashrc
echo 'export PATH=$PATH:$GOPATH/bin' >> ~/.bashrc
source ~/.bashrc

# 验证安装
go version
```

---

## 3. 安装 MySQL 8.0（数据库）

```bash
# 添加 MySQL 官方 APT 仓库
wget https://dev.mysql.com/get/mysql-apt-config_0.8.24-1_all.deb
sudo dpkg -i mysql-apt-config_0.8.24-1_all.deb

# 安装 MySQL 8.0
sudo apt update
sudo apt install -y mysql-server

# 运行安全配置向导（按提示设置 root 密码等）
sudo mysql_secure_installation

# 创建风控平台专用数据库和用户
sudo mysql -u root -p -e "
CREATE DATABASE risk_control CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'riskuser'@'localhost' IDENTIFIED BY 'StrongPassword123!';
GRANT ALL PRIVILEGES ON risk_control.* TO 'riskuser'@'localhost';
FLUSH PRIVILEGES;
"
数据库连接信息（永久保存）
数据库地址（IP）：你的服务器公网 IP / 内网 IP
端口：3306
数据库名：risk_control
用户名：ymy
密码：883684
编码：utf8mb4
排序规则：utf8mb4_unicode_ci



# 启动并设置开机自启
sudo systemctl enable mysql
sudo systemctl start mysql
```

---

## 4. 安装 Redis 6.0+（缓存）

```bash
# Ubuntu 22.04 可直接安装 Redis 6.0+
sudo apt install -y redis-server

# Ubuntu 20.04 用户需先添加仓库：
# sudo add-apt-repository ppa:redislabs/redis
# sudo apt update
# sudo apt install -y redis

# 配置 Redis
sudo sed -i 's/supervised no/supervised systemd/g' /etc/redis/redis.conf
sudo sed -i 's/bind 127.0.0.1 ::1/bind 127.0.0.1/g' /etc/redis/redis.conf

# 启动并设置开机自启
sudo systemctl enable redis-server
sudo systemctl start redis-server

# 验证 Redis
redis-cli ping
```

---

## 5. 安装 Node.js 18.x（前端构建，可选但推荐）

```bash
# 安装 Node.js 18.x（用于可能的前端构建）
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 验证安装
node --version
npm --version
```
v18.20.8
10.8.2
---

## 6. 安装 Nginx（反向代理）

```bash
# 安装 Nginx
sudo apt install -y nginx

# 启动并设置开机自启
sudo systemctl enable nginx
sudo systemctl start nginx

# 验证 Nginx
curl http://localhost
```

---

## 7. 安装 Docker 和 Docker Compose（容器化部署，可选但推荐）

```bash
# 安装 Docker
sudo apt install -y ca-certificates curl gnupg lsb-release
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 安装 Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 将当前用户加入 docker 组（避免每次使用 sudo）
sudo usermod -aG docker $USER

# 重新登录或执行以下命令使组变更生效
newgrp docker

# 验证安装
docker --version
docker-compose --version
```

---

## 8. 安装 Certbot（HTTPS 证书，生产环境必需）

```bash
# 安装 Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取 SSL 证书的命令（替换 your-domain.com 为实际域名）
# sudo certbot --nginx -d your-domain.com
```

---

## 9. 创建专用用户（安全最佳实践）

```bash
# 创建专用系统用户
sudo adduser --system --group --shell /bin/bash riskcontrol
sudo usermod -aG sudo riskcontrol

# 切换到专用用户（后续开发建议在此用户下进行）
su - riskcontrol
```

---

## 10. 验证所有服务状态

```bash
# 检查所有必需服务的状态
sudo systemctl status mysql
sudo systemctl status redis-server
sudo systemctl status nginx

# 检查端口监听情况
sudo ss -tulnp | grep -E ':(3306|6379|80|443)'
```

---

## 11. 防火墙配置（UFW）

```bash
# 启用 UFW 防火墙
sudo ufw enable

# 允许必要端口
sudo ufw allow OpenSSH
sudo ufw allow 'Nginx Full'
sudo ufw allow 3306/tcp  # MySQL（仅内网访问建议）
sudo ufw allow 6379/tcp  # Redis（仅内网访问建议）

# 查看防火墙状态
sudo ufw status verbose
```

---

## 12. 开发目录准备

```bash
# 创建项目根目录（在 riskcontrol 用户下）
mkdir -p ~/projects/risk-control
cd ~/projects/risk-control

# 创建四个微服务目录
mkdir -p auth-service data-service finance-service risk-service

# 创建共享资源目录
mkdir -p shared
```

---

## 总结：已安装的软件清单

| 软件 | 版本 | 用途 |
|------|------|------|
| **Go** | 1.21+ | 后端微服务开发 |
| **MySQL** | 8.0 | 主数据库 |
| **Redis** | 6.0+ | 缓存、会话管理 |
| **Nginx** | 最新版 | 反向代理、静态文件服务 |
| **Node.js** | 18.x | 前端构建（可选） |
| **Docker** | 最新版 | 容器化部署（可选） |
| **Certbot** | 最新版 | HTTPS 证书管理 |
| **Git** | 最新版 | 代码版本控制 |

> **注意**：执行完以上指令后，您的 Ubuntu 系统已经准备好开发和运行私募交易风控平台的所有四个微服务（auth-service、data-service、finance-service、risk-service）。

所有服务都已配置为开机自启，并且按照安全最佳实践进行了基础加固。现在您可以开始按照开发计划逐个实现微服务了。